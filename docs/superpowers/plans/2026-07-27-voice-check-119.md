# 음성 응답 확인·119 자동 신고 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 낙상 확정 후 어르신에게 음성으로 안부를 묻고, 20초까지 대답이 없으면 시스템이 119 신고를 기록해 앱·보호자 웹·감지 페이지 전 화면에 표시한다.

**Architecture:** 감지 페이지에 detector.js와 같은 스타일의 순수 상태머신 `escalation.js`(신규)를 추가하고, 브라우저 음성 API는 `speech.js`(신규) 어댑터로 격리한다. 신고는 새 엔드포인트 없이 같은 낙상 payload에 `reported_119_at`을 붙여 `POST /api/falls/`를 한 번 더 보내고, 서버는 `acknowledgeNow`와 같은 멱등 패턴(기존 값이 null일 때만 쓴다)으로 병합한다. 앱과 보호자 웹은 응답의 `reported_119_at`으로 배지·버튼 잠금을 그린다.

**Tech Stack:** Spring Boot 4(Java 21, Flyway, JUnit 5 + MockMvc + 실 PostgreSQL), 바닐라 ES 모듈 + Web Speech API(웹, Vitest), Flutter/Dart(앱).

설계 근거는 `docs/superpowers/specs/2026-07-27-voice-check-119-design.md`에 있다. 판단이 갈리면 그 문서가 기준이다.

## Global Constraints

- **타임라인은 낙상 확정(FALLEN 진입) 순간이 0초다** — 스펙 원문. "0s 마이크 ON, 5s 보호자 알림(기존 그대로), 10s '괜찮으세요?' 재생, 20s 무응답이면 119 신고." 그리고 "20s는 TTS 재생 시간과 무관한 절대 마감이다." 5초 보호자 알림은 기존 detector(`FALLEN_HOLD` 5000ms → ALERTED)가 이미 하고 있으므로 건드리지 않는다.
- **detector.js는 수정 금지** — 회복 판정(tilt < 45°)을 그대로 쓴다. 에스컬레이션은 detector의 상태 문자열을 읽기만 한다.
- **"NO_PERSON은 회복이 아니다"** — 스펙 원문. "종결은 오직 STANDING(회복), heardOk(해제), 20s 경과(신고)뿐이다."
- **"실패는 전부 신고 쪽으로"** — 스펙 원문. "STT 오류·미지원·마이크 거부는 heardOk가 영영 오지 않는 것, 즉 무응답과 같다. 20s에 신고가 나간다."
- **병합은 멱등** — 스펙 원문. "병합은 `acknowledgeNow`와 같은 멱등 패턴 — 기존 값이 null일 때만 쓴다."
- **새 엔드포인트 금지** — 신고는 `POST /api/falls/` 재-POST뿐이다. "큐 코드 변경은 0줄이다."
- **푸시는 201에만** — 기존 규칙 유지. 컨트롤러의 푸시 분기를 건드리지 않는다.
- **119는 실제 발신이 아니다** — 시스템에 기록되는 신고 상태다. `fall_detail.dart`의 더미 번호(`01000000119`)와 다이얼러 동작은 그대로 둔다.
- **필드 이름** — JSON·DB는 `reported_119_at`, Java는 `reported119At`, Dart는 `reported119At`. 다른 표기를 만들지 않는다.
- **사용자에게 보이는 문구 고정** — 아래 문자열을 그대로 쓴다. 임의로 다듬지 않는다.

  | 자리 | 문구 |
  |------|------|
  | TTS 질문 | `괜찮으세요?` |
  | STT 인식 키워드 | `괜찮` 포함 여부 |
  | 감지 페이지 LISTENING | `🎤 음성 확인 대기` |
  | 감지 페이지 ASKING·WAITING | `괜찮으세요? 확인 중` |
  | 감지 페이지 RESOLVED | `응답 확인 — 신고 안 함` |
  | 감지 페이지 REPORTED | `🚨 119 자동 신고됨` |
  | 앱 상세 문구 | `응답이 없어 119에 자동 신고되었습니다` |
  | 앱·보호자 웹 목록 배지 | `119 신고됨` |

- **한국어** — 사용자에게 보이는 문구와 새로 쓰는 주석은 전부 한국어다. 한국어 문장은 마침표로 끝낸다(뒤에 목록이 와도 콜론으로 끝내지 않는다).
- **새 파일 첫 줄 헤더 주석** — 신규 소스 파일(`escalation.js`, `speech.js`, `V2__*.sql`, 테스트 3개)은 첫 줄에 역할을 밝히는 한국어 한 줄 주석을 단다.
- **수술적 변경** — 내 변경이 틀리게 만든 주석만 고친다. 그 외 인접 코드·주석·포맷은 손대지 않는다.
- **커밋** — 태스크당 1커밋. 한국어 semantic commit 메시지에 아래 트레일러를 붙인다.
  ```
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  ```

**사전 조건.** 백엔드 테스트는 실제 PostgreSQL을 쓴다. 없으면 만든다.

```bash
createdb fall_detection_test   # 이미 있으면 "already exists"가 나고 그냥 넘어가면 된다
```

---

## File Structure

| 파일 | 역할 | 태스크 |
|---|---|---|
| `backend/src/main/resources/db/migration/V2__add_reported_119_at.sql` (신규) | 신고 시각 컬럼 추가 | 1 |
| `backend/src/main/java/com/weniv/falls/domain/FallEvent.java` | 필드·`markReported119` 멱등 세터 | 1 |
| `backend/src/main/java/com/weniv/falls/dto/FallEventRequest.java` | 선택 입력 `reported_119_at` | 1 |
| `backend/src/main/java/com/weniv/falls/dto/FallEventResponse.java` | 응답 노출 | 1 |
| `backend/src/main/java/com/weniv/falls/service/FallService.java` | 신규 반영·중복 병합 | 1 |
| `backend/src/test/java/com/weniv/falls/FallApiTest.java` | 신고 계약 4종 | 1 |
| `web/js/escalation.js` (신규) | 순수 에스컬레이션 상태머신 | 2 |
| `web/tests/escalation.test.js` (신규) | 시나리오 8종 | 2 |
| `web/js/speech.js` (신규) | TTS·STT 어댑터, 자기 목소리 방어 | 3 |
| `web/js/main.js` | 배선 — 명령 분배·신고 재-POST·마이크 권한 | 3 |
| `web/detect.html` | 상태 줄·사생활 문구 | 3 |
| `web/css/style.css` | 상태 줄 스타일 | 3 |
| `app/lib/models.dart` | `reported119At`·`isReported119` | 4 |
| `app/lib/screens/fall_detail.dart` | 119 버튼 잠금·문구·5초 재조회 | 4 |
| `app/lib/screens/fall_list.dart` | `119 신고됨` 배지 | 4 |
| `app/test/models_test.dart` (신규) | 파싱 2종 | 4 |
| `app/test/fall_detail_test.dart` (신규) | 버튼 잠금 2종 | 4 |
| `web/js/guardian.js` | 목록 배지 | 5 |
| `README.md` | 사생활 예외·119 한계·테스트 개수 | 6 |
| `docs/backend-architecture.html` | POST 행·JSON 예시·테스트 개수 | 6 |
| `docs/manual-verification.md` | 9절 — 실제 마이크·스피커 검증 | 6 |
| `checklist.md`, `context-notes.md` | 라운드 기록과 결정 근거 | 6 |

태스크 1이 서버 계약을, 태스크 2가 상태머신 계약을 만든다. 태스크 3은 둘 다의 소비자다. 태스크 4·5는 태스크 1의 응답 필드만 소비하므로 태스크 2·3과 순서를 바꿔도 되지만, 전부 태스크 1에 의존한다.

---

## Task 1: 백엔드 — `reported_119_at` 저장·병합

**Files:**
- Create: `backend/src/main/resources/db/migration/V2__add_reported_119_at.sql`
- Modify: `backend/src/main/java/com/weniv/falls/domain/FallEvent.java`
- Modify: `backend/src/main/java/com/weniv/falls/dto/FallEventRequest.java`
- Modify: `backend/src/main/java/com/weniv/falls/dto/FallEventResponse.java`
- Modify: `backend/src/main/java/com/weniv/falls/service/FallService.java`
- Test: `backend/src/test/java/com/weniv/falls/FallApiTest.java`

**Interfaces:**
- Consumes: 기존 `FallService.create`의 201/200 분기, `uniq_fall_dedup` 유니크 제약, `FallEventRepository.save`.
- Produces:
  - HTTP 계약 — `POST /api/falls/`가 선택 필드 `"reported_119_at"`(ISO-8601, 오프셋 허용)을 받는다. 신규면 201로 저장, 유니크 키 중복이면 200으로 기존 행에 병합(기존 값이 null일 때만 쓴다, 이후 불변). 모든 낙상 응답에 `"reported_119_at"`(값 없으면 null)이 실린다. 태스크 3(재-POST)·4(앱)·5(웹)가 이 계약에 의존한다.
  - `FallEvent.markReported119(Instant at) → void`, `FallEvent.getReported119At() → Instant`
  - `FallEventRequest.reported119At() → OffsetDateTime` (nullable)
- 참고: `FallEventRequest`를 코드에서 직접 생성하는 곳은 없다(Jackson 역직렬화뿐, 테스트도 JSON 문자열을 쓴다). 레코드 컴포넌트를 끝에 추가해도 깨지는 호출부가 없다.

- [ ] **Step 1: 구조를 먼저 만든다 — 마이그레이션·엔티티·DTO**

테스트가 컴파일되려면 접근자가 먼저 있어야 한다(정적 언어 TDD 순서). 로직(FallService)은 아직 건드리지 않으므로 이 단계의 테스트 실패는 컴파일 에러가 아니라 단언 실패가 된다.

`backend/src/main/resources/db/migration/V2__add_reported_119_at.sql`을 새로 만든다.

```sql
-- 음성 무응답 시 119 자동 신고 시각 — null이면 신고 없음
ALTER TABLE fall_event ADD COLUMN reported_119_at TIMESTAMPTZ;
```

`FallEvent.java` — `acknowledgedAt` 필드 선언 바로 아래에 필드를 더한다.

```java
    @Column(name = "reported_119_at")
    private Instant reported119At;
```

`acknowledgeNow()` 메서드 바로 아래에 멱등 세터를 더한다.

```java
    // 첫 신고 시각을 보존한다 — 이미 값이 있으면 무시 (acknowledgeNow와 같은 멱등 패턴)
    // 마이크로초 절단도 같은 이유다: 응답 직렬화 값과 재조회 값이 항상 같아야 한다
    public void markReported119(Instant at) {
        if (reported119At == null && at != null) {
            reported119At = at.truncatedTo(java.time.temporal.ChronoUnit.MICROS);
        }
    }
```

getter 블록 맨 끝(`getAcknowledgedAt` 아래)에 더한다.

```java
    public Instant getReported119At() { return reported119At; }
```

`FallEventRequest.java` — 마지막 컴포넌트 `confidence` 뒤에 선택 컴포넌트를 더한다. 검증 애너테이션이 없다 — 없어도 되는 필드다.

```java
    @NotNull(message = "이 필드는 필수 항목입니다.")
    Double confidence,

    @JsonProperty("reported_119_at")
    OffsetDateTime reported119At) {
```

`FallEventResponse.java` — 레코드 컴포넌트 끝에 더하고 `from`에 반영한다. 바꾼 뒤 전체 모습이다.

```java
public record FallEventResponse(
    Long id,
    @JsonProperty("room_name") String roomName,
    @JsonProperty("room_number") Integer roomNumber,
    @JsonProperty("occurred_at") Instant occurredAt,
    @JsonProperty("created_at") Instant createdAt,
    Double confidence,
    @JsonProperty("acknowledged_at") Instant acknowledgedAt,
    @JsonProperty("reported_119_at") Instant reported119At) {

    public static FallEventResponse from(FallEvent event) {
        return new FallEventResponse(event.getId(), event.getRoomName(), event.getRoomNumber(),
            event.getOccurredAt(), event.getCreatedAt(), event.getConfidence(),
            event.getAcknowledgedAt(), event.getReported119At());
    }
}
```

- [ ] **Step 2: 실패하는 테스트 4개를 쓴다**

`FallApiTest.java`의 일반 import 블록에 한 줄을 더한다. `java.nio.charset.StandardCharsets` 바로 아래가 자리다.

```java
import java.time.Instant;
```

파일 첫 줄 헤더 주석을 통째로 바꾼다.

```java
// 낙상 API 계약 테스트 — 목록 격리·최신순·guardian 강제·acknowledge 멱등·중복 POST 200·신고 병합·푸시 1회/0회·삭제 204/400/404
```

`fallPayload` 헬퍼 바로 아래에 신고 포함 payload 헬퍼를 더한다. `occurred_at`이 `fallPayload`와 같아야 중복 키가 성립한다.

```java
    private String fallPayloadReported(String reportedAt) {
        return "{\"room_name\": \"안방\", \"room_number\": 1, "
            + "\"occurred_at\": \"2026-07-23T03:00:00Z\", \"confidence\": 0.9, "
            + "\"reported_119_at\": \"" + reportedAt + "\"}";
    }
```

클래스 맨 끝(닫는 `}` 앞)에 테스트 4개를 붙인다.

```java
    @Test
    void post_with_reported_119_at_creates_row_with_it() throws Exception {
        // 원본이 큐에 갇혔다가 신고분이 먼저 도착하는 드문 경로 — 신고 재-POST가 행을 만든다
        String body = mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON)
                .content(fallPayloadReported("2026-07-23T03:00:20Z")))
            .andExpect(status().isCreated())
            .andExpect(jsonPath("$.reported_119_at").value("2026-07-23T03:00:20Z"))
            .andReturn().getResponse().getContentAsString(StandardCharsets.UTF_8);
        long id = ((Number) JsonPath.read(body, "$.id")).longValue();
        assertThat(fallEventRepository.findById(id).orElseThrow().getReported119At())
            .isEqualTo(Instant.parse("2026-07-23T03:00:20Z"));
    }

    @Test
    void duplicate_post_merges_reported_119_at_and_sends_no_push() throws Exception {
        // 정상 경로 — 5s 원본 201 뒤에 20s 신고 재-POST가 200으로 병합된다
        String body = mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON).content(fallPayload("0.9")))
            .andExpect(status().isCreated())
            .andReturn().getResponse().getContentAsString(StandardCharsets.UTF_8);
        int firstId = JsonPath.read(body, "$.id");
        clearInvocations(pushService);

        mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON)
                .content(fallPayloadReported("2026-07-23T03:00:20Z")))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.id").value(firstId))
            .andExpect(jsonPath("$.reported_119_at").value("2026-07-23T03:00:20Z"));

        assertThat(fallEventRepository.count()).isEqualTo(1);
        verify(pushService, never()).sendToGuardianAsync(any());   // 200 병합은 푸시가 없다
    }

    @Test
    void reported_119_at_is_immutable_once_set() throws Exception {
        mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON)
                .content(fallPayloadReported("2026-07-23T03:00:20Z")))
            .andExpect(status().isCreated());

        mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON)
                .content(fallPayloadReported("2026-07-23T03:00:59Z")))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.reported_119_at").value("2026-07-23T03:00:20Z"));   // 첫 값 보존
    }

    @Test
    void duplicate_post_without_reported_119_at_keeps_existing_value() throws Exception {
        // 오프라인 큐가 신고 이후 원본을 재전송해도 신고 시각이 지워지면 안 된다
        mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON)
                .content(fallPayloadReported("2026-07-23T03:00:20Z")))
            .andExpect(status().isCreated());

        mockMvc.perform(authed(post("/api/falls/"), guardian)
                .contentType(MediaType.APPLICATION_JSON).content(fallPayload("0.9")))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.reported_119_at").value("2026-07-23T03:00:20Z"));
    }
```

- [ ] **Step 3: 테스트를 돌려 실패를 확인한다**

```bash
cd backend && ./gradlew test --tests 'com.weniv.falls.FallApiTest'
```

기대하는 결과 — 신규 4개 전부 FAIL. `create`가 아직 `reported_119_at`을 무시하므로 응답이 전부 null이고, `jsonPath("$.reported_119_at").value(...)` 단언에서 깨진다. 컴파일 에러가 나면 Step 1이 덜 된 것이니 멈추고 확인한다. 기존 12개는 그대로 PASS여야 한다.

- [ ] **Step 4: `FallService`에 반영·병합을 구현한다**

`create` 메서드와 그 아래를 다음처럼 바꾼다. 두 중복 경로(선조회·경합 재조회)가 같은 `merge`를 지나게 한다.

```java
    public CreateResult create(Guardian guardian, FallEventRequest request) {
        Instant occurredAt = request.occurredAt().toInstant();   // 오프셋 입력 → UTC 변환 저장

        Optional<FallEvent> existing = findDuplicate(guardian, request, occurredAt);
        if (existing.isPresent()) {
            return new CreateResult(merge(existing.get(), request), false);   // 재전송 — 푸시 없음
        }
        try {
            FallEvent event = new FallEvent(
                guardian, request.roomName(), request.roomNumber(), occurredAt,
                request.confidence());
            if (request.reported119At() != null) {
                event.markReported119(request.reported119At().toInstant());
            }
            return new CreateResult(fallEventRepository.saveAndFlush(event), true);
        } catch (DataIntegrityViolationException e) {
            // 생성 경합 — uniq_fall_dedup에 걸렸으면 재조회해 200 경로로
            return new CreateResult(
                merge(findDuplicate(guardian, request, occurredAt).orElseThrow(), request), false);
        }
    }

    // 신고 시각 병합 — 기존 값이 null일 때만 쓴다 (markReported119가 보장). 쓸 것이 없으면 저장도 없다.
    private FallEvent merge(FallEvent event, FallEventRequest request) {
        if (request.reported119At() == null || event.getReported119At() != null) {
            return event;
        }
        event.markReported119(request.reported119At().toInstant());
        return fallEventRepository.save(event);
    }
```

파일 첫 줄 헤더 주석을 통째로 바꾼다.

```java
// 낙상 등록·조회·확인·삭제 — 오프라인 큐 재전송 멱등성 (같은 낙상 재수신 → 기존 행에 신고 시각만 병합, 푸시 없음)
```

- [ ] **Step 5: 테스트를 돌려 통과를 확인한다**

```bash
cd backend && ./gradlew test --tests 'com.weniv.falls.FallApiTest'
```

기대: 16개 전부 PASS(기존 12 + 신규 4).

- [ ] **Step 6: 백엔드 전체를 돌려 회귀가 없는지 본다**

```bash
cd backend && ./gradlew test
```

기대: 43개 PASS. 이전이 39개였으므로 정확히 4개가 늘어야 한다. 숫자가 다르면 멈추고 원인을 본다.

- [ ] **Step 7: 커밋**

```bash
git add backend/src/main/resources/db/migration/V2__add_reported_119_at.sql \
        backend/src/main/java/com/weniv/falls/domain/FallEvent.java \
        backend/src/main/java/com/weniv/falls/dto/FallEventRequest.java \
        backend/src/main/java/com/weniv/falls/dto/FallEventResponse.java \
        backend/src/main/java/com/weniv/falls/service/FallService.java \
        backend/src/test/java/com/weniv/falls/FallApiTest.java
git commit -F - <<'EOF'
feat: 낙상 이벤트에 119 자동 신고 시각 — 재-POST 병합, 첫 값 보존

reported_119_at 컬럼(V2)을 더하고 POST /api/falls/가 선택 필드로 받는다.
신규면 저장, 유니크 키 중복이면 acknowledgeNow와 같은 멱등 패턴으로
기존 행에 병합한다 — 기존 값이 null일 때만 쓴다. 푸시는 종전대로 201에만
나가므로 정상 경로의 신고 재-POST(200)에 중복 푸시가 없다.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

## Task 2: `escalation.js` — 순수 에스컬레이션 상태머신

**Files:**
- Create: `web/js/escalation.js`
- Test: `web/tests/escalation.test.js`

**Interfaces:**
- Consumes: detector가 매 프레임 내놓는 상태 문자열 `"NO_PERSON" | "STANDING" | "FALLING" | "FALLEN" | "ALERTED"`. 브라우저 API는 쓰지 않는다(detector.js와 같은 규칙).
- Produces:
  - `export const CONFIG = { QUESTION_AT: 10_000, REPORT_AT: 20_000 }`
  - `export function createEscalation(config = CONFIG)` → 객체:
    - `update(detectorState, t) → { state, commands, statusText }` — `state`는 `"IDLE" | "LISTENING" | "ASKING" | "WAITING" | "RESOLVED" | "REPORTED"`, `commands`는 `"MIC_ON" | "PLAY_QUESTION" | "REPORT" | "MIC_OFF"`의 배열(한 번만 실행할 명령), `statusText`는 화면 문구(IDLE이면 null — 화면이 숨긴다).
    - `heardOk() → void` — 어댑터가 "괜찮"을 들었을 때 주입.
    - `ttsEnded() → void` — 어댑터가 TTS 종료(실패·스킵 포함) 시 주입.
    - `get state()`
  - 태스크 3(main.js)이 이 계약에 의존한다.

- [ ] **Step 1: 실패하는 테스트 8개를 쓴다**

`web/tests/escalation.test.js`를 새로 만든다. detector 테스트와 달리 입력이 상태 문자열뿐이라 헬퍼를 파일 안에 둔다. 프레임 간격은 100ms다.

```js
// escalation.js 상태머신의 음성 확인→119 신고 시나리오 검증

import { describe, expect, it } from "vitest";

import { createEscalation } from "../js/escalation.js";

// t0~t1(미포함)을 100ms 간격 프레임으로 만든다. events는 { 시각: "heardOk" | "ttsEnded" }다.
function frames(t0, t1, ds, events = {}) {
  const out = [];
  for (let t = t0; t < t1; t += 100) out.push({ t, ds, event: events[t] });
  return out;
}

const standing = (t0, t1) => frames(t0, t1, "STANDING");
const fallen = (t0, t1, events) => frames(t0, t1, "FALLEN", events);
const alerted = (t0, t1, events) => frames(t0, t1, "ALERTED", events);
const noPerson = (t0, t1) => frames(t0, t1, "NO_PERSON");

function run(esc, steps) {
  const commands = [];
  const states = [];
  for (const { t, ds, event } of steps) {
    if (event) esc[event]();
    const r = esc.update(ds, t);
    for (const c of r.commands) commands.push({ t, c });
    states.push({ t, state: r.state, statusText: r.statusText });
  }
  return {
    states,
    times: (c) => commands.filter((x) => x.c === c).map((x) => x.t),
    textAt: (t) => states.find((s) => s.t === t)?.statusText,
    saw: (state) => states.some((s) => s.state === state),
  };
}

// 모든 시나리오의 에피소드 시작(FALLEN 진입)은 t=1000이다. detector가 실제로 내놓는
// 순서를 흉내 내 FALLEN 5초 뒤에는 ALERTED(보호자 알림 후)를 준다.

describe("escalation", () => {
  it("낙상 확정 0s에 MIC_ON, 10s에 PLAY_QUESTION이 정확히 1회 나간다", () => {
    const esc = createEscalation();
    const r = run(esc, [...standing(0, 1000), ...fallen(1000, 6000), ...alerted(6000, 13000)]);

    expect(r.times("MIC_ON")).toEqual([1000]);
    expect(r.times("PLAY_QUESTION")).toEqual([11000]); // 에피소드 시작 + 10초
    expect(r.textAt(1000)).toBe("🎤 음성 확인 대기");
    expect(r.textAt(11000)).toBe("괜찮으세요? 확인 중");
    expect(esc.state).toBe("ASKING"); // ttsEnded가 아직 없다
  });

  it("무응답이면 20s에 REPORT 정확히 1회, MIC_OFF와 함께 나간다", () => {
    const esc = createEscalation();
    const r = run(esc, [
      ...standing(0, 1000),
      ...fallen(1000, 6000),
      ...alerted(6000, 26000, { 11500: "ttsEnded" }),
    ]);

    expect(r.times("REPORT")).toEqual([21000]); // 에피소드 시작 + 20초 절대 마감
    expect(r.times("MIC_OFF")).toEqual([21000]);
    expect(esc.state).toBe("REPORTED");
    expect(r.textAt(21000)).toBe("🚨 119 자동 신고됨");
  });

  it("질문 전(0~10s) '괜찮아'는 질문을 생략하고 RESOLVED — 신고 없음", () => {
    const esc = createEscalation();
    const r = run(esc, [
      ...standing(0, 1000),
      ...fallen(1000, 6000, { 4000: "heardOk" }),
      ...alerted(6000, 26000),
    ]);

    expect(esc.state).toBe("RESOLVED");
    expect(r.times("PLAY_QUESTION")).toEqual([]); // 질문 생략
    expect(r.times("REPORT")).toEqual([]);
    expect(r.times("MIC_OFF")).toEqual([4000]);
    expect(r.textAt(4000)).toBe("응답 확인 — 신고 안 함");
  });

  it("질문 후 대기(WAITING) 중 '괜찮아'면 RESOLVED — 신고 없음", () => {
    const esc = createEscalation();
    const r = run(esc, [
      ...standing(0, 1000),
      ...fallen(1000, 6000),
      ...alerted(6000, 26000, { 11500: "ttsEnded", 15000: "heardOk" }),
    ]);

    expect(esc.state).toBe("RESOLVED");
    expect(r.times("PLAY_QUESTION")).toEqual([11000]);
    expect(r.times("REPORT")).toEqual([]);
    expect(r.times("MIC_OFF")).toEqual([15000]);
  });

  it("어느 단계든 일어나면(STANDING) 전체 취소·MIC_OFF, 이후 아무 일도 없다", () => {
    // 청취 중 회복 — FALLEN에서 tilt가 풀리면 detector가 곧장 STANDING을 준다
    const esc1 = createEscalation();
    const r1 = run(esc1, [...standing(0, 1000), ...fallen(1000, 4000), ...standing(4000, 26000)]);
    expect(esc1.state).toBe("IDLE");
    expect(r1.times("MIC_OFF")).toEqual([4000]);
    expect(r1.times("PLAY_QUESTION")).toEqual([]);
    expect(r1.times("REPORT")).toEqual([]);
    expect(r1.textAt(4000)).toBe(null); // IDLE에서는 상태 줄을 숨긴다

    // 대답 대기 중 회복
    const esc2 = createEscalation();
    const r2 = run(esc2, [
      ...standing(0, 1000),
      ...fallen(1000, 6000),
      ...alerted(6000, 15000, { 11500: "ttsEnded" }),
      ...standing(15000, 26000),
    ]);
    expect(esc2.state).toBe("IDLE");
    expect(r2.times("MIC_OFF")).toEqual([15000]);
    expect(r2.times("REPORT")).toEqual([]);
  });

  it("NO_PERSON(가려짐)은 회복이 아니다 — 에피소드가 유지되고 20s에 신고된다", () => {
    const esc = createEscalation();
    // ttsEnded도 영영 오지 않는 최악 경로 — 20s 절대 마감은 ASKING에서도 신고한다
    const r = run(esc, [...standing(0, 1000), ...fallen(1000, 3000), ...noPerson(3000, 26000)]);

    expect(r.times("PLAY_QUESTION")).toEqual([11000]); // 가려진 동안에도 질문은 나간다
    expect(r.times("REPORT")).toEqual([21000]);
    expect(esc.state).toBe("REPORTED");
  });

  it("신고 후 일어나기 전까지 새 에피소드가 없고, 일어나면 재무장된다", () => {
    const esc = createEscalation();
    const r = run(esc, [
      ...standing(0, 1000),
      ...fallen(1000, 6000),
      ...alerted(6000, 22000, { 11500: "ttsEnded" }), // 21000에 REPORTED
      ...fallen(22000, 24000), // 누운 채 뒤척여 FALLEN이 다시 보여도
      ...standing(24000, 25000), // 일어나야만 재무장된다
      ...fallen(25000, 26000), // 새 낙상
    ]);

    expect(r.times("REPORT")).toEqual([21000]); // 누워 있는 동안 중복 신고 없음
    expect(r.times("MIC_ON")).toEqual([1000, 25000]); // 재무장 후에만 새 에피소드
    expect(esc.state).toBe("LISTENING");
  });

  it("ASKING(TTS 재생) 중 heardOk는 버린다 — 늦게 도착한 자기 목소리 인식 방어", () => {
    const esc = createEscalation();
    // 11000에 질문 시작. 11300의 heardOk는 청취 정지 직전 오디오의 늦은 인식 결과다.
    const r = run(esc, [
      ...standing(0, 1000),
      ...fallen(1000, 6000),
      ...alerted(6000, 26000, { 11300: "heardOk", 12000: "ttsEnded" }),
    ]);

    expect(r.saw("RESOLVED")).toBe(false); // 버려졌고, WAITING으로 넘어가서도 되살아나지 않는다
    expect(r.times("REPORT")).toEqual([21000]);
    expect(esc.state).toBe("REPORTED");
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

```bash
cd web && npx vitest run tests/escalation.test.js
```

기대: `Failed to resolve import "../js/escalation.js"` 류의 로드 에러로 전부 실패한다.

- [ ] **Step 3: `web/js/escalation.js`를 구현한다**

```js
// 낙상 확정 후 음성 확인→119 신고 에스컬레이션 순수 상태머신 (브라우저 API 미사용)

export const CONFIG = {
  QUESTION_AT: 10_000, // ms — 에피소드 시작(FALLEN 진입) 후 질문 재생 시점
  REPORT_AT: 20_000, // ms — 무응답 신고 마감. TTS 재생 시간과 무관한 절대 마감이다
};

// IDLE의 null은 "상태 줄을 숨겨라"다
const STATUS_TEXT = {
  IDLE: null,
  LISTENING: "🎤 음성 확인 대기",
  ASKING: "괜찮으세요? 확인 중",
  WAITING: "괜찮으세요? 확인 중",
  RESOLVED: "응답 확인 — 신고 안 함",
  REPORTED: "🚨 119 자동 신고됨",
};

export function createEscalation(config = CONFIG) {
  const c = { ...CONFIG, ...config };

  let state = "IDLE";
  let startedAt = null;
  let heardOkPending = false;
  let ttsEndedPending = false;

  const inEpisode = () => state === "LISTENING" || state === "ASKING" || state === "WAITING";

  function update(detectorState, t) {
    const commands = [];
    const heardOk = heardOkPending;
    const ttsEnded = ttsEndedPending;
    // ASKING이 소비 없이 버리는 것까지 포함해, 이벤트는 어느 상태에서든 한 프레임만 산다
    heardOkPending = false;
    ttsEndedPending = false;

    if (inEpisode() && detectorState === "STANDING") {
      // 일어났다 — 어느 단계든 전체 취소다. NO_PERSON은 여기 오지 않으므로 에피소드가 유지된다.
      state = "IDLE";
      commands.push("MIC_OFF");
    } else {
      switch (state) {
        case "IDLE":
          // 종결 뒤라면 STANDING(재무장)을 거쳐서만 돌아오므로, FALLEN이 보이면 곧 새 에피소드다
          if (detectorState === "FALLEN") {
            state = "LISTENING";
            startedAt = t;
            commands.push("MIC_ON");
          }
          break;

        case "LISTENING":
          if (heardOk) {
            state = "RESOLVED"; // 질문(10s) 전이면 질문도 생략된다
            commands.push("MIC_OFF");
          } else if (t - startedAt >= c.QUESTION_AT) {
            state = "ASKING";
            commands.push("PLAY_QUESTION");
          }
          break;

        case "ASKING":
          // heardOk는 버린다 — 어댑터가 청취를 꺼두지만, 정지 직전 오디오의 늦은 인식 결과가
          // 자기 질문("괜찮으세요?")일 수 있어 상태머신 차원에서도 이중으로 방어한다.
          if (t - startedAt >= c.REPORT_AT) {
            state = "REPORTED"; // ttsEnded가 영영 안 와도 절대 마감이 신고한다
            commands.push("REPORT", "MIC_OFF");
          } else if (ttsEnded) {
            state = "WAITING"; // TTS 실패·스킵도 같은 경로 — 어댑터가 즉시 ttsEnded를 넣는다
          }
          break;

        case "WAITING":
          // 마감 프레임에 함께 도착한 heardOk는 응답으로 인정한다 — 대답은 마감 전에 나왔다
          if (heardOk) {
            state = "RESOLVED";
            commands.push("MIC_OFF");
          } else if (t - startedAt >= c.REPORT_AT) {
            state = "REPORTED";
            commands.push("REPORT", "MIC_OFF");
          }
          break;

        case "RESOLVED":
        case "REPORTED":
          // 일어나야만 재무장된다 — 누워 있는 동안 중복 신고가 없는 이유다 (ALERTED 재무장과 같은 규칙)
          if (detectorState === "STANDING") state = "IDLE";
          break;
      }
    }

    return { state, commands, statusText: STATUS_TEXT[state] };
  }

  return {
    update,
    heardOk() {
      heardOkPending = true;
    },
    ttsEnded() {
      ttsEndedPending = true;
    },
    get state() {
      return state;
    },
  };
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

```bash
cd web && npx vitest run tests/escalation.test.js
```

기대: 8개 전부 PASS.

- [ ] **Step 5: 웹 전체를 돌려 회귀가 없는지 본다**

```bash
cd web && npm test
```

기대: 30개 PASS(기존 22 + 신규 8). 숫자가 다르면 멈추고 원인을 본다.

- [ ] **Step 6: 커밋**

```bash
git add web/js/escalation.js web/tests/escalation.test.js
git commit -F - <<'EOF'
feat: 음성 확인→119 신고 에스컬레이션 순수 상태머신

FALLEN 진입 0s 기준으로 10s 질문·20s 절대 마감 신고를 전이한다.
STANDING만이 취소이고 NO_PERSON은 에피소드를 유지한다. ASKING 중
heardOk는 자기 질문의 늦은 인식 결과일 수 있어 버린다(이중 방어).
종결 후에는 일어나야만 재무장돼 누워 있는 동안 중복 신고가 없다.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

## Task 3: `speech.js` + 감지 페이지 배선

**Files:**
- Create: `web/js/speech.js`
- Modify: `web/js/main.js`
- Modify: `web/detect.html`
- Modify: `web/css/style.css`

**Interfaces:**
- Consumes: 태스크 1의 HTTP 계약(`reported_119_at` 재-POST), 태스크 2의 `createEscalation`. 기존 `postFall(payload)`, `queue.enqueue(payload)`, `flushQueue()`, `showBanner(message)`.
- Produces: `export function createSpeechAdapter({ onHeardOk, onTtsEnded })` → `{ requestMicPermission(), startListening(), stopListening(), playQuestion() }`. 전부 반환값 없음(권한 요청만 Promise).

**이 태스크에 자동 테스트는 없다.** `speech.js`와 `main.js`는 브라우저 API(음성 인식·합성, getUserMedia, DOM) 코드라 기존 테스트 하네스가 없다 — `pose.js`·`overlay.js`와 같은 취급이다. 검증은 구문 검사와 회귀(`npm test`), 태스크 6의 수동 절차로 한다.

- [ ] **Step 1: `web/js/speech.js`를 만든다**

```js
// 음성 어댑터 — TTS 질문 재생과 STT("괜찮") 인식. 자기 목소리 방어와 자동 재시작을 맡는다.

const QUESTION = "괜찮으세요?";
const OK_KEYWORD = "괜찮"; // "괜찮아", "괜찮아요", "나 괜찮아" 전부 걸린다

export function createSpeechAdapter({ onHeardOk, onTtsEnded }) {
  const SR = window.SpeechRecognition ?? window.webkitSpeechRecognition;
  let recognition = null; // 살아 있는 인식 세션 (없으면 null)
  let listening = false; // 에피소드 동안 true — onend 자동 재시작의 근거
  let paused = false; // TTS 재생 동안 true — 자기 질문을 알아듣지 않게 청취를 끈다

  function startRecognition() {
    if (!SR || recognition || !listening || paused) return;
    const r = new SR();
    r.lang = "ko-KR";
    r.continuous = true;
    r.interimResults = true; // 중간 결과에서 "괜찮"이 잡혀도 바로 해제한다
    r.onresult = (e) => {
      for (let i = e.resultIndex; i < e.results.length; i += 1) {
        if (e.results[i][0].transcript.includes(OK_KEYWORD)) {
          onHeardOk();
          return;
        }
      }
    };
    r.onend = () => {
      // 침묵·일시 오류로 끊겨도 에피소드가 살아 있으면 다시 듣는다.
      // 마이크 거부처럼 시작이 계속 실패해도 그냥 조용하다 — 무응답과 같아 20초에 신고가 나간다.
      recognition = null;
      startRecognition();
    };
    recognition = r;
    try {
      r.start();
    } catch {
      recognition = null;
    }
  }

  function stopRecognition() {
    if (!recognition) return;
    const r = recognition;
    recognition = null;
    r.onend = null; // 의도한 정지는 자동 재시작하지 않는다
    try {
      r.stop();
    } catch {
      // 이미 죽어 있어도 상관없다
    }
  }

  return {
    // 감지 시작 클릭 때 카메라와 함께 미리 받아 둔다 — 응급 순간에 권한 팝업이 뜨면 안 된다.
    // 받자마자 트랙을 꺼서 평상시에는 마이크가 완전히 꺼져 있게 한다.
    async requestMicPermission() {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
        for (const track of stream.getTracks()) track.stop();
      } catch {
        // 거부·미지원은 무응답과 같다 — 확인 실패가 신고를 삼키지 않는다
      }
    },

    startListening() {
      listening = true;
      paused = false;
      startRecognition();
    },

    stopListening() {
      listening = false;
      paused = false;
      stopRecognition();
    },

    // 질문 재생. 시작 전에 청취를 멈추고 끝난 뒤 재개한다 — 질문 "괜찮으세요?"에도
    // "괜찮"이 들어 있어, 켜 둔 채 재생하면 시스템이 자기 질문을 알아듣고 신고를 취소한다.
    playQuestion() {
      paused = true;
      stopRecognition();
      let finished = false;
      const finish = () => {
        if (finished) return;
        finished = true;
        paused = false;
        startRecognition(); // 에피소드가 이미 끝났으면(listening=false) 안에서 무시된다
        onTtsEnded();
      };
      if (!window.speechSynthesis || typeof SpeechSynthesisUtterance === "undefined") {
        finish(); // TTS 미지원 — 질문을 생략하고 바로 대답 대기로 넘어간다
        return;
      }
      const utterance = new SpeechSynthesisUtterance(QUESTION);
      utterance.lang = "ko-KR";
      utterance.onend = finish;
      utterance.onerror = finish;
      speechSynthesis.speak(utterance);
    },
  };
}
```

- [ ] **Step 2: `web/detect.html`에 상태 줄을 더하고 사생활 문구를 고친다**

`#metrics` 줄 바로 아래(전송 카운트 위)에 상태 줄을 끼운다. 바꾸기 전.

```html
        <p class="metric" id="metrics"></p>
        <p class="metric" id="sent">전송된 낙상 0건</p>
```

바꾼 뒤.

```html
        <p class="metric" id="metrics"></p>
        <p id="escalation" class="hidden"></p>
        <p class="metric" id="sent">전송된 낙상 0건</p>
```

`.privacy` 블록의 문구를 고친다. 마이크가 생겼는데 화면 문구가 예전 주장 그대로면 화면이 거짓말을 하게 된다. 바꾸기 전.

```html
        <div class="privacy">
          검은 배경 위 스켈레톤이 전부입니다. 영상과 관절 좌표는 이 브라우저를 벗어나지 않습니다.
        </div>
```

바꾼 뒤.

```html
        <div class="privacy">
          검은 배경 위 스켈레톤이 전부입니다. 영상과 관절 좌표는 이 브라우저를 벗어나지 않습니다.
          마이크는 낙상 확인 응급 구간에만 켜지고, 그 음성은 브라우저 음성 인식(구글 서버)으로 처리됩니다.
        </div>
```

- [ ] **Step 3: `web/css/style.css`에 상태 줄 스타일을 더한다**

`.privacy` 규칙 바로 위에 붙인다. 응급 진행 상태라 `.metric`처럼 흐리게 두지 않는다.

```css
/* 음성 확인·119 신고 진행 상태 — 응급 구간(IDLE 제외)에만 보인다 */
#escalation {
  font-size: 15px;
  font-weight: 600;
}
```

- [ ] **Step 4: `web/js/main.js`를 배선한다**

import 블록에 두 줄을 더한다(경로 알파벳 순서 — `detector` 다음 `escalation`, `queue` 다음 `speech`).

```js
import { createRoom, listRooms, postFall, requireToken } from "./api.js";
import { createDetector } from "./detector.js";
import { createEscalation } from "./escalation.js";
import { drawSkeleton } from "./overlay.js";
import { createPoseLandmarker, runLoop, startCamera } from "./pose.js";
import { createFallQueue } from "./queue.js";
import { createSpeechAdapter } from "./speech.js";
import { createTuningRecorder } from "./tuning.js";
```

`el` 맵의 `metrics`와 `sent` 사이에 한 줄을 끼운다.

```js
  metrics: document.getElementById("metrics"),
  escalation: document.getElementById("escalation"),
  sent: document.getElementById("sent"),
```

모듈 수준 `let sentCount = 0;` 바로 아래에 에스컬레이션 상태를 더한다.

```js
const escalation = createEscalation();
const speech = createSpeechAdapter({
  onHeardOk: () => escalation.heardOk(),
  onTtsEnded: () => escalation.ttsEnded(),
});
let lastFallPayload = null; // 현재 에피소드에서 확정 전송한 낙상 — 신고 재-POST의 바탕
let lastFallingAt = null; // 마지막 FALLING 진입 시각 — 확정 전에 가려진 채 신고에 이르는 드문 경로용
let prevDetectorState = null;
```

감지 시작 핸들러에서 `startCamera` try/catch 성공 직후, `el.room.textContent` 줄 앞에 마이크 권한 요청을 끼운다.

```js
  // 응급 순간에 권한 팝업이 뜨면 안 된다 — 카메라에 이어 미리 받아 두고 트랙은 바로 끈다.
  // 거부해도 감지는 그대로 진행한다. STT가 영영 못 들으면 무응답과 같아 20초에 신고가 나간다.
  await speech.requestMicPermission();
```

같은 핸들러 안, `runLoop(...)` 호출 바로 앞에 신고 전송 함수를 둔다(`room`이 이 스코프에 있다).

```js
  // 20s 무응답 — 같은 낙상 payload에 신고 시각만 붙여 한 번 더 보낸다. 서버가 기존 행에 병합한다.
  const reportEmergency = (t) => {
    // 확정(5s) 전부터 계속 가려져 원본 전송이 아직 없는 드문 경로에서도 신고는 나가야 한다.
    // occurred_at을 원본과 같게 맞춰야 나중에 원본이 도착해도 서버가 한 행으로 합친다.
    const base = lastFallPayload ?? {
      room_name: room.name,
      room_number: room.number,
      occurred_at: new Date(performance.timeOrigin + lastFallingAt).toISOString(),
      confidence: 0,
    };
    const payload = { ...base, reported_119_at: new Date(performance.timeOrigin + t).toISOString() };
    postFall(payload)
      .then(() => flushQueue())
      .catch(() => {
        // 신고도 같은 큐를 탄다 — 연결이 돌아오면 재전송되고 서버가 병합한다
        queue.enqueue(payload);
        showBanner("전송 실패 — 저장해 두었다가 연결되면 다시 보냅니다");
      });
  };
```

`runLoop` 콜백을 통째로 바꾼다. 바꾼 뒤 전체 모습이다 — 기존 `if (fall)` 블록 안에는 `lastFallPayload = payload;` 한 줄만 늘었고, 그 아래 에스컬레이션 구간이 새로 붙었다.

```js
  runLoop(landmarker, el.video, (landmarks, t) => {
    const { state, fall, tilt, hipVelocity } = detector.update(landmarks, t);

    // 확정(5s) 전에 신고 마감이 오는 드문 경로를 위해, 실제로 넘어진 시각을 따로 기억한다
    if (state === "FALLING" && prevDetectorState !== "FALLING") lastFallingAt = t;
    prevDetectorState = state;

    drawSkeleton(ctx, landmarks, state);
    el.state.textContent = state;
    el.metrics.textContent = `tilt ${(tilt ?? 0).toFixed(1)}°  ·  hipV ${hipVelocity.toFixed(2)}/s`;
    tuning.record(t, state, tilt, hipVelocity);

    if (fall) {
      const payload = {
        room_name: room.name,
        room_number: room.number,
        occurred_at: new Date(performance.timeOrigin + fall.occurredAt).toISOString(),
        confidence: fall.confidence,
      };
      lastFallPayload = payload; // 이 에피소드의 신고 재-POST가 이 payload를 바탕으로 삼는다
      postFall(payload)
        .then(() => {
          sentCount += 1;
          el.sent.textContent = `전송된 낙상 ${sentCount}건`;
          flushQueue(); // 방금 성공했으니 밀려 있던 것도 지금 보낸다
        })
        .catch(() => {
          // 401 로그아웃 중이어도 적재해 둔다 — 재로그인 후 flush가 되살리므로 손해가 없다
          queue.enqueue(payload);
          showBanner("전송 실패 — 저장해 두었다가 연결되면 다시 보냅니다");
        });
    }

    const esc = escalation.update(state, t);
    el.escalation.textContent = esc.statusText ?? "";
    el.escalation.classList.toggle("hidden", !esc.statusText);
    for (const command of esc.commands) {
      if (command === "MIC_ON") {
        lastFallPayload = null; // 새 에피소드 — 이전 낙상의 payload가 신고에 섞이면 안 된다
        speech.startListening();
      } else if (command === "PLAY_QUESTION") {
        speech.playQuestion();
      } else if (command === "REPORT") {
        reportEmergency(t);
      } else if (command === "MIC_OFF") {
        speech.stopListening();
      }
    }
  });
```

- [ ] **Step 5: 구문 검사와 회귀를 확인한다**

```bash
cd web && node --check js/speech.js && node --check js/main.js && node --check js/escalation.js && echo "구문 OK"
cd web && npm test
```

기대: `구문 OK`, 테스트 30개 그대로 PASS(이 태스크는 순수 모듈을 건드리지 않는다).

- [ ] **Step 6: 커밋**

```bash
git add web/js/speech.js web/js/main.js web/detect.html web/css/style.css
git commit -F - <<'EOF'
feat: 감지 페이지 음성 확인 배선 — TTS 질문·STT 청취·119 신고 재-POST

speech.js가 TTS 재생 동안 인식을 멈춰 자기 질문("괜찮으세요?" 안의
"괜찮")을 알아듣는 함정을 막고, 침묵으로 끊긴 인식은 에피소드 동안
자동 재시작한다. 마이크 권한은 감지 시작 클릭에서 미리 받는다.

신고는 같은 낙상 payload에 reported_119_at을 붙인 재-POST다. 실패하면
기존 localStorage 큐에 실린다(큐 코드 변경 0줄). 확정 전송 전에 가려진
채 마감이 온 드문 경로는 FALLING 진입 시각으로 occurred_at을 맞춰
서버가 나중 원본과 한 행으로 합칠 수 있게 한다.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

## Task 4: 앱 — 신고 상태 반영

**Files:**
- Modify: `app/lib/models.dart`
- Modify: `app/lib/screens/fall_detail.dart`
- Modify: `app/lib/screens/fall_list.dart`
- Test: `app/test/models_test.dart` (신규)
- Test: `app/test/fall_detail_test.dart` (신규)

**Interfaces:**
- Consumes: 태스크 1의 응답 필드 `"reported_119_at"`. 기존 `Api.listFalls() → Future<List<FallEvent>>`, `Api.getProfile()`, `UnauthorizedException`.
- Produces: `FallEvent.reported119At → DateTime?`, `FallEvent.isReported119 → bool`. 화면 계약 — 신고된 이벤트의 상세는 119 버튼 비활성 + 문구 `응답이 없어 119에 자동 신고되었습니다`, 목록 trailing은 `119 신고됨`.

- [ ] **Step 1: 모델 구조를 만든다**

`app/lib/models.dart`의 `FallEvent`에 필드·파싱·getter를 더한다. 필드 선언(`acknowledgedAt` 아래).

```dart
  final DateTime? acknowledgedAt;
  final DateTime? reported119At;
```

생성자 파라미터(`this.acknowledgedAt,` 아래).

```dart
    this.acknowledgedAt,
    this.reported119At,
```

`fromJson`의 `acknowledgedAt:` 항목 아래.

```dart
        reported119At: json['reported_119_at'] == null
            ? null
            : DateTime.parse(json['reported_119_at'] as String).toLocal(),
```

`isAcknowledged` getter 아래.

```dart
  bool get isReported119 => reported119At != null;
```

- [ ] **Step 2: 모델 테스트를 쓴다**

`app/test/models_test.dart`를 새로 만든다. 구조가 곧 구현인 모델이라 red 단계는 컴파일 에러뿐이다 — 이 테스트는 파싱 계약을 고정하는 용도이고, 행동의 red는 Step 3의 위젯 테스트가 맡는다.

```dart
// 서버 JSON → FallEvent 파싱 검증 — reported_119_at 유무

import 'package:fall_guardian/models.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  final base = <String, dynamic>{
    'id': 1,
    'room_name': '안방',
    'room_number': 1,
    'occurred_at': '2026-07-27T03:00:00Z',
    'created_at': '2026-07-27T03:00:05Z',
    'confidence': 0.9,
    'acknowledged_at': null,
  };

  test('reported_119_at 키가 아예 없으면(구버전 응답) 신고 안 된 상태다', () {
    final e = FallEvent.fromJson(base);

    expect(e.reported119At, isNull);
    expect(e.isReported119, isFalse);
  });

  test('reported_119_at이 있으면 로컬 시각으로 파싱되고 신고 상태다', () {
    final e = FallEvent.fromJson({...base, 'reported_119_at': '2026-07-27T03:00:20Z'});

    expect(e.isReported119, isTrue);
    expect(e.reported119At, DateTime.parse('2026-07-27T03:00:20Z').toLocal());
  });
}
```

실행해 둘 다 PASS인지 본다.

```bash
cd app && flutter test test/models_test.dart
```

- [ ] **Step 3: 실패하는 위젯 테스트를 쓴다**

`app/test/fall_detail_test.dart`를 새로 만든다. 네트워크 없이 화면을 띄우기 위해 `Api`를 서브클래스로 덮는다(상세 화면이 부르는 것은 `getProfile`과 — 이 태스크에서 붙일 — `listFalls`뿐이다).

```dart
// 상세 화면 119 버튼 잠금 검증 — 신고된 이벤트에서 비활성화와 문구 표시

import 'package:fall_guardian/api.dart';
import 'package:fall_guardian/models.dart';
import 'package:fall_guardian/screens/fall_detail.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

class _FakeApi extends Api {
  @override
  Future<Profile> getProfile() async => const Profile(elderPhone: '');

  @override
  Future<List<FallEvent>> listFalls() async => [];
}

FallEvent _event({DateTime? reported119At}) => FallEvent(
      id: 1,
      roomName: '안방',
      roomNumber: 1,
      occurredAt: DateTime(2026, 7, 27, 12),
      createdAt: DateTime(2026, 7, 27, 12),
      confidence: 0.9,
      reported119At: reported119At,
    );

// OutlinedButton.icon은 OutlinedButton의 비공개 서브클래스를 만들므로 byType으로는 못 찾는다
Finder _emergencyButton() => find.ancestor(
      of: find.text('119 신고 (시연용 더미 번호)'),
      matching: find.byWidgetPredicate((w) => w is OutlinedButton),
    );

void main() {
  testWidgets('신고된 이벤트는 119 버튼이 잠기고 자동 신고 문구가 보인다', (tester) async {
    await tester.pumpWidget(MaterialApp(
      home: FallDetailScreen(
        api: _FakeApi(),
        event: _event(reported119At: DateTime(2026, 7, 27, 12, 0, 20)),
      ),
    ));
    await tester.pump();

    expect(tester.widget<OutlinedButton>(_emergencyButton()).onPressed, isNull);
    expect(find.text('응답이 없어 119에 자동 신고되었습니다'), findsOneWidget);
  });

  testWidgets('신고 전에는 119 버튼이 활성이고 문구가 없다', (tester) async {
    await tester.pumpWidget(MaterialApp(
      home: FallDetailScreen(api: _FakeApi(), event: _event()),
    ));
    await tester.pump();

    expect(tester.widget<OutlinedButton>(_emergencyButton()).onPressed, isNotNull);
    expect(find.text('응답이 없어 119에 자동 신고되었습니다'), findsNothing);
  });
}
```

- [ ] **Step 4: 위젯 테스트를 돌려 실패를 확인한다**

```bash
cd app && flutter test test/fall_detail_test.dart
```

기대: 첫 테스트 FAIL — `onPressed`가 null이 아니고 문구도 없다(화면이 아직 `isReported119`를 모른다). 둘째 테스트는 현재 동작 그대로라 PASS다 — 구현 뒤 회귀 방지용으로 둔다.

- [ ] **Step 5: `fall_detail.dart`에 잠금·문구·5초 재조회를 구현한다**

import 맨 앞에 `dart:async`를 더한다(`package:` import들보다 위다).

```dart
import 'dart:async';

import 'package:flutter/material.dart';
```

`bool _busy = false;` 아래에 타이머 필드를 더한다.

```dart
  Timer? _refreshTimer;
```

`initState`를 바꾸고 `dispose`와 `_refetch`를 더한다. 바꾼 뒤 모습이다.

```dart
  @override
  void initState() {
    super.initState();
    widget.api.getProfile().then((p) {
      if (mounted) setState(() => _elderPhone = p.elderPhone);
    }).catchError((_) {
      // 못 불러오면 미등록으로 취급한다. 버튼만 비활성화되고 화면은 정상 동작한다.
      if (mounted) setState(() => _elderPhone = '');
    });
    // 화면을 보는 동안 119 자동 신고가 도착하면 실시간으로 잠기도록 5초마다 다시 읽는다.
    // 단건 GET이 없어 목록을 받아 자기 id를 찾는다.
    _refreshTimer = Timer.periodic(const Duration(seconds: 5), (_) => _refetch());
  }

  @override
  void dispose() {
    _refreshTimer?.cancel();
    super.dispose();
  }

  Future<void> _refetch() async {
    try {
      final events = await widget.api.listFalls();
      final idx = events.indexWhere((e) => e.id == _event.id);
      // _busy 중에는 덮어쓰지 않는다 — 확인·삭제 진행 중 상태가 폴링과 엇갈리면 안 된다.
      if (idx != -1 && mounted && !_busy) setState(() => _event = events[idx]);
    } on UnauthorizedException {
      // 목록 화면의 폴러가 곧 같은 401을 받아 로그아웃을 처리한다. 여기서는 조용히 둔다.
    } catch (_) {
      // 일시 오류 — 다음 주기가 다시 시도한다.
    }
  }
```

119 버튼을 바꾸고 문구를 붙인다. 바꾸기 전.

```dart
          OutlinedButton.icon(
            onPressed: () => _dial(_emergencyPhone),
            icon: const Icon(Icons.local_hospital),
            label: const Text('119 신고 (시연용 더미 번호)'),
          ),
```

바꾼 뒤.

```dart
          OutlinedButton.icon(
            // 자동 신고가 이미 나갔으면 잠근다 — "이미 신고됨"을 보여주는 자리다.
            onPressed: _event.isReported119 ? null : () => _dial(_emergencyPhone),
            icon: const Icon(Icons.local_hospital),
            label: const Text('119 신고 (시연용 더미 번호)'),
          ),
          if (_event.isReported119)
            Padding(
              padding: const EdgeInsets.only(top: 8),
              child: Text(
                '응답이 없어 119에 자동 신고되었습니다',
                style: TextStyle(color: Theme.of(context).colorScheme.error),
              ),
            ),
```

- [ ] **Step 6: `fall_list.dart`에 배지를 더한다**

`ListTile`의 trailing을 바꾼다. 바꾸기 전.

```dart
                                trailing: Text(e.isAcknowledged ? '확인함' : '미확인'),
```

바꾼 뒤. 확인 여부는 leading 아이콘이 계속 보여주므로, trailing은 더 급한 정보인 신고 배지가 우선한다.

```dart
                                trailing: e.isReported119
                                    ? Text(
                                        '119 신고됨',
                                        style: TextStyle(
                                          color: Theme.of(context).colorScheme.error,
                                          fontWeight: FontWeight.bold,
                                        ),
                                      )
                                    : Text(e.isAcknowledged ? '확인함' : '미확인'),
```

- [ ] **Step 7: 정적 검사와 전체 테스트를 돌린다**

```bash
cd app && flutter analyze
cd app && flutter test
```

기대: `No issues found!`, 테스트 11개 PASS(기존 7 + 모델 2 + 상세 2). `use_build_context_synchronously` 경고가 나오면 무시하지 말고 본다 — `_refetch`의 `setState`는 `mounted` 검사 뒤다.

- [ ] **Step 8: 커밋**

```bash
git add app/lib/models.dart app/lib/screens/fall_detail.dart app/lib/screens/fall_list.dart \
        app/test/models_test.dart app/test/fall_detail_test.dart
git commit -F - <<'EOF'
feat: 앱에 119 자동 신고 상태 — 상세 버튼 잠금·문구, 목록 배지, 5초 재조회

reported_119_at이 있으면 상세의 119 버튼을 잠그고 "응답이 없어 119에
자동 신고되었습니다"를 보여준다. 화면을 보는 중 신고가 도착해도 5초
재조회(목록에서 자기 id 찾기 — 단건 GET이 없다)가 실시간으로 잠근다.
목록 trailing은 확인 여부보다 급한 "119 신고됨" 배지가 우선한다.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

## Task 5: 보호자 웹 — 목록 배지

**Files:**
- Modify: `web/js/guardian.js`

**Interfaces:**
- Consumes: 태스크 1의 응답 필드 `reported_119_at`.
- Produces: 없음(화면 표시뿐).

**이 태스크에 자동 테스트는 없다.** `guardian.js`는 DOM·네트워크 코드라 하네스가 없다(기록 삭제 라운드와 같은 판단). 검증은 구문 검사·회귀와 태스크 6의 수동 절차로 한다.

- [ ] **Step 1: `refreshFalls`의 라벨에 배지를 더한다**

바꾸기 전.

```js
    label.textContent =
      `${f.room_name} ${f.room_number} · ${fmt(f.occurred_at)} · ` +
      (f.acknowledged_at ? "확인함" : "미확인");
```

바꾼 뒤.

```js
    label.textContent =
      `${f.room_name} ${f.room_number} · ${fmt(f.occurred_at)} · ` +
      (f.acknowledged_at ? "확인함" : "미확인") +
      (f.reported_119_at ? " · 119 신고됨" : "");
```

- [ ] **Step 2: 구문 검사와 회귀를 확인한다**

```bash
cd web && node --check js/guardian.js && echo "구문 OK"
cd web && npm test
```

기대: `구문 OK`, 30개 그대로 PASS.

- [ ] **Step 3: 커밋**

```bash
git add web/js/guardian.js
git commit -F - <<'EOF'
feat: 보호자 페이지 낙상 목록에 "119 신고됨" 배지

바쁜 보호자가 뒤늦게 목록만 봐도 자동 신고 사실을 알 수 있어야 한다.
5초 폴링이 이미 목록을 다시 그리므로 라벨에 조건 문자열 하나면 된다.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

## Task 6: 문서 — 사생활 예외·수동 검증·기록

**Files:**
- Modify: `README.md`
- Modify: `docs/backend-architecture.html`
- Modify: `docs/manual-verification.md`
- Modify: `checklist.md`
- Modify: `context-notes.md`

**Interfaces:**
- Consumes: 태스크 1~5의 완성된 동작.
- Produces: 없음(문서).

- [ ] **Step 1: `README.md` 사생활 보호 절에 예외를 명시한다**

"## 사생활 보호" 절의 세 번째 불릿(디버그용 영상 토글도 없다) 바로 아래에 문단을 더한다.

```markdown
**유일한 예외 — 응급 구간의 마이크(2026-07-27 추가).** 낙상이 확정되면 감지 페이지가 마이크를 켜 10초에 "괜찮으세요?"를 묻고, 20초까지 대답이 없으면 119 신고를 기록한다. 이 구간(낙상 확정~해제·신고·회복)의 음성은 브라우저 내장 음성 인식(Web Speech API)을 거쳐 구글 서버로 전송된다. 평상시에는 마이크가 꺼져 있고 권한만 미리 받아 둔다. 영상은 여전히 어떤 순간에도 기기를 벗어나지 않는다. 온디바이스 STT는 과제 범위에 무겁고, 쓰러진 사람의 생명이 음성 몇 초의 사생활보다 우선한다고 판단해 설계한 예외다.
```

- [ ] **Step 2: `README.md` 구조 도식의 전송 줄을 고친다**

바꾸기 전.

```
           └─ 낙상 확정 시에만 1회
              POST /api/falls/  (Token 헤더, 재전송 중복은 서버가 흡수)
```

바꾼 뒤.

```
           └─ 낙상 확정 시 1회 + 20초 무응답 시 119 신고 재-POST 1회
              POST /api/falls/  (Token 헤더, 재전송·신고 중복은 서버가 흡수·병합)
```

- [ ] **Step 3: `README.md` 테스트 개수와 119 한계 불릿을 고친다**

테스트 절 세 줄을 바꾼다.

```markdown
cd backend && ./gradlew test    # 43개 — 인증·소유권·방·프로필·푸시·전송 멱등성·기록 삭제·119 신고 병합 (fall_detection_test DB 필요: createdb fall_detection_test)
cd web     && npm test          # 30개 — 상태머신 시나리오(낙상·음성 에스컬레이션) + 오프라인 큐
cd app     && flutter test      # 11개 — 새 이벤트 판별 4, 알림 설정 3, 모델 파싱 2, 상세 119 버튼 2
```

"알려진 한계"의 119 불릿을 바꾼다. 바꾸기 전.

```markdown
- **119는 더미 번호** — 시연 중 실제 발신을 막기 위함이다. `fall_detail.dart`의 `_emergencyPhone` 참고.
```

바꾼 뒤.

```markdown
- **119는 실제 발신이 아니다** — 수동 버튼은 더미 번호(`fall_detail.dart`의 `_emergencyPhone`)이고, 음성 무응답 자동 신고(2026-07-27 추가)도 발신 없이 `reported_119_at` 기록과 화면 표시까지다. 시연 중 실제 신고를 막기 위함이다.
```

- [ ] **Step 4: `docs/backend-architecture.html`을 갱신한다**

엔드포인트 표의 `POST /api/falls/` 행을 바꾼다. 바꾸기 전.

```html
    <tr><td><span class="pill post">POST</span><code>/api/falls/</code></td><td>낙상 등록. 신규 201 + 푸시 / 재전송 200 + 푸시 없음.</td></tr>
```

바꾼 뒤.

```html
    <tr><td><span class="pill post">POST</span><code>/api/falls/</code></td><td>낙상 등록. 신규 201 + 푸시 / 재전송 200 + 푸시 없음. <code>reported_119_at</code>이 있으면 저장·병합(첫 값 보존).</td></tr>
```

응답 JSON 예시의 마지막 두 줄을 바꾼다(쉼표에 주의). 바꾸기 전.

```
  "confidence": 0.94,
  "acknowledged_at": null                    <span class="c">// 아직 보호자가 확인 전</span>
```

바꾼 뒤.

```
  "confidence": 0.94,
  "acknowledged_at": null,                   <span class="c">// 아직 보호자가 확인 전</span>
  "reported_119_at": null                    <span class="c">// 음성 무응답 시 119 자동 신고 시각</span>
```

테스트 개수를 갱신한다 — 세 자리 전부다.

- 개요 표의 `<td>39개 (8절)</td>` → `<td>43개 (8절)</td>`
- 제목 `<h3>테스트 — 39개, 전부 실제 DB에 붙는다</h3>` → `<h3>테스트 — 43개, 전부 실제 DB에 붙는다</h3>`
- 실행 예시 `<span class="c"># 39 tests, 0 failures</span>` → `<span class="c"># 43 tests, 0 failures</span>`
- 테스트 표의 `FallApiTest` 행 — 개수 `12` → `16`, 설명을 `멱등 POST, 201/200 분기, 소유권, acknowledge 멱등, 오프셋 → UTC 변환, 삭제 204/400/404, 신고 병합·불변`으로 바꾼다.

- [ ] **Step 5: `docs/manual-verification.md`에 9절을 더한다**

파일 맨 끝에 붙인다.

````markdown
## 9. 음성 응답 확인·119 자동 신고 (2026-07-27 라운드)

자동 테스트가 대신 밟는 것 — 상태머신 타이밍·취소·재무장(web 에스컬레이션 8), 신고 병합·불변(backend 4), 모델 파싱·버튼 잠금(app 4). 남은 것은 실제 마이크·스피커·TTS·STT다. Web Speech API가 필요하므로 Chrome에서 한다. 시나리오마다 넘어져야 하니 매트리스를 깔고 한다.

### 감지 페이지 — 타임라인

- [ ] 감지 시작 클릭 시 카메라에 이어 **마이크 권한도 미리** 묻는다. 이후 응급 구간에 권한 팝업이 다시 뜨지 않는다
- [ ] 빠르게 넘어져 FALLEN이 되는 순간 상태 줄 **"🎤 음성 확인 대기"**가 나타난다
- [ ] 10초에 스피커에서 **"괜찮으세요?"**가 나오고 상태 줄이 **"괜찮으세요? 확인 중"**으로 바뀐다
- [ ] 그대로 침묵하면 20초에 **"🚨 119 자동 신고됨"**이 뜬다 (넘어진 순간부터 20초다 — 질문 후 20초가 아니다)
- [ ] (일어나서 재무장 후 새 에피소드) 질문이 나온 뒤 "괜찮아"라고 답하면 **"응답 확인 — 신고 안 함"**이 뜨고 신고가 나가지 않는다
- [ ] (새 에피소드) 질문 전(10초 안)에 "괜찮아" → **질문 없이** 바로 해제된다
- [ ] (새 에피소드) 도중에 일어나면 상태 줄이 사라지고 신고도 질문도 없다
- [ ] **자기 목소리 함정** — 질문 "괜찮으세요?" 재생 직후에도 "확인 중"이 유지된다. 재생 순간 해제되면 시스템이 자기 질문을 알아들은 것이다
- [ ] 사이트 설정에서 마이크를 "차단"으로 바꾸고 새 에피소드 → 그래도 20초에 신고가 나간다 (확인 실패는 무응답과 같다)

일어나야만 다음 에피소드가 재무장된다. 누운 채 반복하면 안 된다.

### 신고 반영 — 세 화면

신고를 하나 만든 뒤 확인한다.

```bash
psql -d fall_detection -c \
  "select id, occurred_at, reported_119_at from fall_event order by id desc limit 3;"
```

- [ ] DB의 `reported_119_at`이 현재 시각 근처다
- [ ] **앱 상세를 열어 둔 채** 신고가 도착하면 5초 안에 119 버튼이 잠기고 **"응답이 없어 119에 자동 신고되었습니다"**가 나타난다 (재조회를 눈으로 확인하는 항목이다)
- [ ] 앱 목록에 **"119 신고됨"** 배지가 붉게 뜬다
- [ ] 보호자 페이지 목록에도 5초 안에 **"119 신고됨"**이 붙는다
- [ ] 잠긴 119 버튼은 눌리지 않는다
````

- [ ] **Step 6: `checklist.md`에 19절을 더한다**

파일 맨 끝에 붙인다(마지막 절이 `## 18`인지 먼저 확인하고, 다르면 다음 번호를 쓴다).

```markdown
## 19. 음성 응답 확인·119 자동 신고 (2026-07-27)

- [x] 설계 — 낙상 확정 0s 기준 타임라인(10s 질문·20s 절대 마감), Web Speech API 채택, 새 엔드포인트 없는 재-POST 병합
- [x] 백엔드 — reported_119_at 컬럼(V2)·요청/응답 노출·멱등 병합(첫 값 보존), 계약 테스트 4종
- [x] escalation.js — 순수 상태머신(IDLE→LISTENING→ASKING→WAITING→REPORTED/RESOLVED), 시나리오 8종
- [x] speech.js — TTS·STT 어댑터, 자기 목소리 방어(재생 중 청취 정지 + 상태머신 이중 방어), 침묵 자동 재시작, 권한 선요청
- [x] 감지 페이지 — 상태 줄(대기→확인 중→해제/신고), 신고 재-POST, 실패 시 기존 큐 적재(큐 변경 0줄)
- [x] 앱 — 상세 119 버튼 잠금+문구, 5초 재조회로 실시간 잠김, 목록 배지, 테스트 4종
- [x] 보호자 웹 — 목록 "119 신고됨" 배지
- [x] 문서 — README 사생활 예외·구조 도식, 아키텍처 문서, 테스트 개수 39/22/7 → 43/30/11
- [ ] 수동 검증 — 실제 마이크·스피커로 `docs/manual-verification.md` 9절 (사람이 밟는다)
```

- [ ] **Step 7: `context-notes.md`에 결정 근거를 더한다**

파일 맨 끝에 붙인다.

```markdown
## 음성 응답 확인·119 자동 신고 (2026-07-27)

**타임라인은 낙상 확정(FALLEN 진입)이 0초다.** 0s 마이크 ON → 5s 보호자 알림(기존 그대로) → 10s "괜찮으세요?" → 20s 무응답 신고. 보호자 알림 시점부터 다시 세는 안은 신고가 25초로 늦어져 기각했다. 20초는 TTS 재생과 무관한 절대 마감이라, ttsEnded가 영영 안 와도(TTS가 조용히 죽어도) 신고가 나간다.

**STT는 Web Speech API다.** 응급 구간(낙상 확정~해제·신고·회복)에만 마이크가 켜지는 조건으로, 인식 오디오가 구글 서버를 거치는 트레이드오프를 감수했다. 온디바이스(Vosk, 모델 ~50MB)는 과제 범위에 무겁고, 음량 기반 감지는 고통의 신음을 "괜찮음"으로 오인해 신고를 취소하는 치명적 결함이 있어 기각했다.

**자기 목소리 방어는 이중이다.** 질문 "괜찮으세요?"에 인식 키워드 "괜찮"이 들어 있다. 어댑터가 TTS 재생 동안 인식을 끄는 것이 1차, 상태머신이 ASKING 중 heardOk를 버리는 것이 2차다. 정지 직전 오디오의 인식 결과가 정지 후에 도착하는 경로가 있어 어댑터만으로는 못 막는다.

**신고는 새 엔드포인트 없이 재-POST다.** 같은 낙상 payload에 reported_119_at을 붙여 POST /api/falls/를 한 번 더 보낸다. 유니크 키 중복(200)이면 acknowledgeNow와 같은 멱등 병합 — 기존 값이 null일 때만 쓴다. 실패하면 기존 localStorage 큐가 그대로 실어 나른다(큐 코드 변경 0줄). 두 POST의 도착 순서가 뒤집혀도(신고분이 먼저 201, 원본이 나중에 200) 결과가 같고, 그 드문 경로의 푸시는 결함이 아니라 지연된 첫 알림이다.

**확정 전송 없이 신고에 이르는 드문 경로를 막지 않고 메웠다.** FALLEN 직후부터 계속 가려지면(NO_PERSON) detector의 5초 확정이 일어나지 못해 원본 payload가 없는 채 20초 마감이 온다. 에피소드를 끊는 대신(스펙 — NO_PERSON은 회복이 아니다), main.js가 FALLING 진입 시각을 따로 기억해 occurred_at을 원본과 같게 만들어 신고를 보낸다 — 시각이 같아야 나중에 원본이 도착해도 서버가 한 행으로 합친다. confidence는 확인할 길이 없어 0이다.

**실패는 전부 신고 쪽으로.** STT 오류·미지원·마이크 거부·TTS 실패는 heardOk가 영영 안 오는 것, 즉 무응답이라 20초에 신고된다. 확인 장치의 실패가 신고를 삼키면 안전망이 아니라는 판단이다. 대가는 오신고 가능성인데, 이 시스템의 신고는 실제 발신이 아니라 기록이라 감수할 만하다.
```

- [ ] **Step 8: 커밋**

```bash
git add README.md docs/backend-architecture.html docs/manual-verification.md checklist.md context-notes.md
git commit -F - <<'EOF'
docs: 음성 확인·119 신고 라운드 기록 — 사생활 예외·수동 검증 9절·결정 근거

README 사생활 절에 유일한 예외(응급 구간 마이크, Web Speech API 경유)를
명시하고, 자기 목소리 이중 방어·절대 마감·재-POST 병합·가려진 채 신고
경로의 판단 근거를 context-notes에 남겼다.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

## 최종 검증

여섯 태스크가 끝난 뒤 한 번에 돌린다.

```bash
cd backend && ./gradlew test          # 43개 PASS
cd ../web  && npm test                # 30개 PASS
cd ../app  && flutter analyze         # No issues found!
cd ../app  && flutter test            # 11개 PASS
```

그다음 `docs/manual-verification.md` 9절을 실제로 한 번 밟는다. 특히 **자기 목소리 함정**(질문 재생 직후 해제되지 않는가)과 **20초 절대 마감**(질문 시점이 아니라 넘어진 순간 기준인가)은 자동 테스트가 실제 오디오 경로까지는 못 보는 자리다. 백엔드를 켠 채 감지→신고→앱·웹 반영까지 한 흐름으로 확인한다.
