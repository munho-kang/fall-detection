# 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 덧붙인다. 다음 세션이 결정을 다시 유도하지 않아도 되게 하는 것이 목적이다.

---

## 2026-07-17 — 설계 확정

### 감지를 브라우저에서 돌린다

서버에서 MediaPipe Python을 쓰는 게 구현은 익숙하지만, 그러면 영상이 네트워크를 탄다. 이 프로젝트의 명분이 사생활 보호이므로 정면으로 충돌한다. 브라우저에서 전부 처리하면 영상이 기기를 벗어나지 않는 것이 코드 구조로 보장된다.

랜드마크만 서버로 스트리밍하는 중간안도 검토했으나, 영상이 안 나가는 건 같으면서 트래픽과 복잡도만 늘어서 버렸다.

### 화면에 원본 영상을 표시하지 않는다

검은 배경 + 스켈레톤만. 디버그용 영상 토글도 넣지 않기로 했다. 사생활 보호를 눈으로 보여주는 것이 과제에서 가장 강한 포인트라, 토글이 있으면 그 메시지가 흐려진다.

### FCM 대신 폴링

FCM이 실제 제품에 맞지만, Firebase 프로젝트 생성 · google-services.json · 서버 키 · Play 서비스 있는 기기까지 깨질 지점이 너무 많다. 과제 시연에서 알림이 안 뜨면 프로젝트 전체가 실패로 보인다. 폴링은 외부 의존성이 0이고 에뮬레이터에서 바로 돈다. 대신 백그라운드 알림이 안 되는 한계를 README에 명시한다.

### Room 테이블을 만들지 않는다

방 등록 화면을 범위 밖으로 뒀으므로 Room 테이블이 있어도 시드 데이터로만 채워진다. 방 정보를 FallEvent에 직접 넣어 테이블 하나로 끝냈다. 방 관리 기능이 나중에 필요해지면 그때 분리한다.

### 목록 API가 폴링을 겸한다

`since` 파라미터를 두는 대신, 앱이 마지막으로 본 id를 로컬에 들고 응답의 최대 id와 비교한다. 엔드포인트가 하나로 줄고 목록 화면과 폴링이 같은 코드를 쓴다.

최초 로그인 시 기존 이벤트 전부에 알림이 쏟아지는 함정이 있다. 첫 응답의 최대 id는 알림 없이 저장하고 시작한다.

### 세 관문 (속도 → 자세 → 시간)

낙상 감지의 진짜 난제는 "넘어짐"과 "그냥 누움"의 구분이다. 단순 임계값(몸통이 수평이면 낙상)은 침대에 눕는 것도 전부 잡아서 시연 중 오탐지가 터진다.

- **속도** — 천천히 눕기를 거른다. 이게 1차 관문이라 가장 중요하다.
- **자세** — 급히 앉는 것을 거른다.
- **시간** — 스스로 일어난 사람 때문에 보호자를 깨우지 않는다.

학습 모델도 검토했으나 데이터셋 수집 · 라벨링 · 학습 · 변환이 과제 범위를 넘고, 데이터 없이는 규칙 기반보다 못하다.

### occurred_at은 FALLING 진입 시각

ALERTED 시각을 쓰면 보호자에게 실제보다 5초 늦은 시각이 표시된다. 판정은 나중에 확정되더라도 기록하는 시각은 실제로 넘어진 순간이어야 한다.

### detector.js를 순수 함수로 분리

MediaPipe · DOM · 네트워크를 하나도 안 쓰는 모듈로 뺐다. 웹캠 없이 가짜 랜드마크 시퀀스로 단위 테스트가 가능해진다. 브라우저는 같은 파일을 `<script type="module">`로 그대로 쓴다. Vitest는 dev 의존성일 뿐이고 배포 코드는 순수 JS다.

작업 순서에서 detector.js(2번)를 감지 페이지(3번)보다 먼저 두는 이유도 같다. 상태머신이 검증돼 있으면 3번에서 문제가 생겼을 때 원인이 랜드마크 품질임을 바로 안다.

### 낙상 POST 유실을 감수한다

재시도 3회 후 포기. 실제 제품이라면 절대 안 되는 동작이고 localStorage 큐잉이 맞지만, 과제 범위에서는 재시도 + 배너까지만 하고 README에 한계로 적기로 했다.

### 119는 더미 번호

시연 중 실제 발신되면 큰일이다. 상수를 더미로 두고 코드에 이유를 주석으로 남긴다.

---

## 2026-07-17 — 구현 계획 작성 중 검증한 것들

계획을 쓰기 전에 스크래치패드에서 실제로 돌려본 결과다. 계획의 코드 블록은 전부 아래 검증을 통과한 것이다.

### 툴체인 실측

Python 3.14.4 / Node 25.8.1 / Flutter 3.44.0 / Dart 3.12.0. `pip install django`는 **Django 6.0.7 + DRF 3.17.1**을 가져온다. Python 3.14에서 정상 동작하고 `authtoken` 마이그레이션도 통과한다. 설계 당시엔 버전을 못 박지 않았는데, 실측값을 계획에 고정했다.

### Django 6.0의 startproject 템플릿은 홑따옴표를 쓴다

`config/urls.py`가 `path('admin/', ...)`로 생성된다. 쌍따옴표를 가정하고 문자열 치환을 하면 **조용히 실패**하고 모든 라우트가 404가 된다. 스모크 테스트에서 실제로 이걸로 5개 테스트가 깨졌다. 계획에는 치환 대신 파일 전체 내용을 적었다.

### Android 에뮬레이터가 이 기기에 없다

`flutter emulators` → 없음. iOS 시뮬레이터(iPhone 17 Pro 등)만 있다. 설계 5절의 `10.0.2.2:8000`은 Android 전용 주소이고, iOS 시뮬레이터는 호스트 네트워크를 공유하므로 `127.0.0.1:8000`이다. 둘 다 살리려고 `api.dart`에서 `Platform.isAndroid` 분기 한 줄로 처리하기로 했다. 양쪽 플랫폼이 실제로 다 사정권에 있으므로 투기적 유연성이 아니다.

### resetMotion()은 방어선이 아니다 — 진짜 방어선은 Δt 계산이다

상태머신을 구현한 뒤 뮤테이션 테스트를 돌렸다. 속도 관문 제거 · `occurred_at`을 확정 시각으로 변경 · ALERTED 미전이는 전부 테스트를 깨뜨렸다(= 테스트에 이빨이 있다). 그런데 **`resetMotion()`을 통째로 지워도 7개가 전부 통과했다.**

이유를 파보니, `NO_PERSON`은 정의상 2초 이상 미검출일 때만 발동하므로 복귀 시점의 Δt가 항상 2000ms 이상이다. 좌표가 최대치(0.3)로 점프해도 가짜 속도는 `0.3 / 2.1s = 0.14/s`라 임계값 0.45에 한참 못 미친다.

설계 6절이 경고한 "재검출 순간의 좌표 점프가 거대한 가짜 속도로 잡힌다"는 **Δt를 고정 프레임 간격(33ms)으로 놓는 구현에서만** 터진다. 그 경우 `0.3 / 0.033 = 9.0/s`로 임계값의 20배다.

그래서 계획에서 강조하는 규칙은 `resetMotion()`이 아니라 **"Δt는 반드시 실제 타임스탬프 차이로 계산한다"**로 바꿨다. `resetMotion()`은 설계에 명시돼 있고 비용이 없으므로 심층 방어로 남기되, 이게 오탐지를 막는 근거라고 착각하지 않도록 주석과 계획에 적었다.

### MediaPipe CDN 버전

설계에는 버전이 없었다. `@mediapipe/tasks-vision`의 최신은 **0.10.35**이고, 아래 셋 다 200을 확인했다.

- `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/vision_bundle.mjs`
- `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/wasm`
- `https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task`

버전을 안 박고 `@latest`를 쓰면 브레이킹 체인지에 시연이 깨질 수 있어 고정했다.

### performance.now()를 그대로 occurred_at으로 보내면 안 된다

`detector.js`가 다루는 타임스탬프는 `performance.now()` 기준(페이지 로드 이후 경과 ms)이다. 이걸 그대로 서버에 보내면 1970년이 찍힌다. `new Date(performance.timeOrigin + occurredAt).toISOString()`으로 벽시계 시각으로 변환해야 한다. 계획 11번 태스크에 명시했다.

---

## 임계값 튜닝 기록

설계상의 초기값은 전부 추정치다. 실제 웹캠 테스트 후 조정한 내역을 여기에 남긴다.

측정 조건도 같이 적는다. 카메라 배치가 빠지면 아래 숫자는 재현할 수 없다.

- 카메라 배치 (높이 / 거리 / 각도):
- 측정일:

| 항목 | 초기값 | 조정값 | 이유 |
|---|---|---|---|
| FALL_VELOCITY | 0.45 /s | | |
| TILT_UPRIGHT | 45° | | |
| TILT_FALLEN | 60° | | |
| FALLING_WINDOW | 1000ms | | |
| FALLEN_HOLD | 5000ms | | |
| EMA_ALPHA | 0.4 | | |

### 2026-07-17 — 실측 미실시, 초기값 그대로 둔다

Task 16 진행 시점에서 실제 웹캠 앞에 서서 값을 재는 작업(Step 1~3)은 사람이 직접 해야 하는 수동 검증이라 이 세션에서는 수행할 수 없었다. 그래서 위 표는 채우지 못했고, `detector.js`의 `CONFIG`도 손대지 않았다 — 실측 없이 숫자를 바꾸는 것은 튜닝이 아니라 추측이기 때문이다.

즉 현재 `CONFIG` 값(FALL_VELOCITY 0.45/s, TILT_UPRIGHT 45°, TILT_FALLEN 60°, FALLING_WINDOW 1000ms, FALLEN_HOLD 5000ms, EMA_ALPHA 0.4)은 전부 설계 단계의 추정치이며, 실측으로 검증된 적이 없다. 실제 웹캠 앞에서 아래를 재기 전까지는 이 값들을 신뢰할 수 없다.

- 가만히 서 있을 때 `tilt` 범위와 `hipV` 노이즈 폭 — 이게 0선 근처에서 얼마나 튀는지 모르면 임계값이 노이즈에 묻히는지 알 수 없다.
- 침대에 천천히 눕는 동안의 `hipV` 최대치 — `FALL_VELOCITY`는 이 값보다 **커야** 한다.
- 의자에 급히 앉는 동안의 `hipV` 최대치와 `tilt` 최대치 — 앉는 동작이 속도 관문을 넘어와도 자세 관문(`TILT_FALLEN`)에서 걸러지는지 확인해야 한다.
- 빠르게 넘어질 때의 `hipV` 최대치 — `FALL_VELOCITY`는 이 값보다 **작아야** 감지가 된다.
- 바닥에 누워 있을 때의 `tilt` 범위 — `TILT_FALLEN`이 이 범위 안에 들어와야 FALLEN 상태가 유지된다.

핵심은 `FALL_VELOCITY`가 "천천히 눕기 최대치"와 "빠르게 넘어지기 최대치" 사이에 실제로 들어가는지다. 두 구간이 겹치면 속도만으로는 구분이 안 되는 것이고, 계획 문서(Step 1)가 지적한 대로 카메라 각도 문제일 가능성이 크다.

이 실측과 3회 연속 오탐지 없는 라이브 검증(계획 Step 3)은 이 프로젝트의 수동 검증 체크리스트로 넘긴다. `checklist.md`에도 해당 항목을 미완료로 남겨 뒀다.

---

## 2026-07-17 — 튜닝을 시작할 수 있게 측정 도구를 붙였다

실측을 하려고 절차를 다시 읽다가 계획서·체크리스트가 **실행 불가능한 절차를 적어놨다**는 것을 발견했다. "화면의 `tilt`/`hipV` 표시를 보며 잰다"고 되어 있는데, 그 표시는 30fps로 갱신되는 텍스트이고 빠른 낙상은 300ms(≈9프레임) 만에 끝난다. 눈으로 피크를 읽을 수 없고, 읽어야 할 순간에 측정자는 바닥에 누워 있다.

그래서 `web/js/tuning.js`를 추가했다. 시행별 피크를 화면에 **고정 표시**하고(일어난 뒤 읽는다), `r` 키로 시행을 끊고, 전 프레임 시계열을 CSV로 내보낸다. `detector.js`는 한 줄도 건드리지 않았다 — 측정 도구가 판정 로직에 스며들면 순수 모듈이라는 전제가 깨진다. 튜닝이 끝나면 이 파일과 호출부만 지우면 된다.

### 계획서의 판정 기준이 틀렸다

계획 Task 16 Step 1과 기존 체크리스트는 `FALL_VELOCITY`를 "천천히 눕기 최대치와 빠르게 넘어지기 **최대치** 사이"에 두라고 했다. 이건 틀렸다. 넘어지기를 5회 하면 피크가 5개인데, 임계값이 그중 최대치보다만 작으면 **가장 빠르게 넘어진 1회만 감지되고 나머지 4회는 놓친다.** 낙상 감지에서 미탐지는 오탐지보다 치명적이므로 그냥 넘어갈 오차가 아니다.

맞는 규칙은 `max(눕기·앉기 모든 시행의 피크) < FALL_VELOCITY < min(넘어지기 모든 시행의 피크)`다. 오탐지 쪽은 최댓값, 미탐지 쪽은 최솟값을 본다. 그래서 동작마다 5회씩 반복해야 하고 — 1회씩 재면 두 분포가 겹치는지 자체를 알 수 없다 — 체크리스트를 이 기준으로 고쳤다.

### 임계값은 카메라 배치와 한 세트다

`hipVelocity`는 정규화 좌표(0~1) 기준이라 몸이 프레임에서 차지하는 비율에 그대로 비례한다. 카메라를 2배 멀리 두면 같은 속도로 넘어져도 hipV가 절반이 된다. `0.45/s`는 물리 상수가 아니라 **특정 카메라 배치에서만 유효한 숫자**다. 그래서 튜닝 표에 카메라 높이·거리·각도 칸을 만들었다. 이게 없으면 다음 세션이 카메라를 옮기는 순간 튜닝이 무효가 되는데 아무도 그걸 모른다.

### tilt의 사각지대 — 실측으로 확인해야 한다

`detector.js`의 tilt는 `atan2(|dx|, |dy|)`로 이미지 평면에 투영된 어깨-엉덩이 벡터의 각도다. 카메라 **정면이나 정반대 방향으로** 넘어지면 몸통이 z축을 향해 투영 벡터가 짧아지고 dx가 0에 가까워진다. 즉 **바닥에 완전히 누워 있는데 tilt가 0°에 가깝게 나올 수 있고**, 그러면 2차 관문을 못 넘어 FALLEN에 도달하지 못한다.

아직 추론일 뿐이라 단정하지 않는다. 체크리스트에 "옆 / 정면 / 대각선 방향으로 누워 tilt 범위 측정"을 넣었다. 정면에서 60°를 못 넘기면 `TILT_FALLEN`을 낮춰서 해결할 일이 아니다 — 낮추면 앉기 오탐지가 터진다. 카메라를 옆에서 보는 각도로 옮기거나 README의 한계로 명시하는 게 맞다.
