# 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 덧붙인다. 다음 세션이 결정을 다시 유도하지 않아도 되게 하는 것이 목적이다.

---

## 2026-07-17 — 설계 확정

### 감지를 브라우저에서 돌린다

서버에서 MediaPipe Python을 쓰는 게 구현은 익숙하지만, 그러면 영상이 네트워크를 탄다. 이 프로젝트의 명분이 사생활 보호이므로 정면으로 충돌한다. 브라우저에서 전부 처리하면 영상이 기기를 벗어나지 않는 것이 코드 구조로 보장된다.

랜드마크만 서버로 스트리밍하는 중간안도 검토했으나, 영상이 안 나가는 건 같으면서 트래픽과 복잡도만 늘어서 버렸다.

### 화면에 원본 영상을 표시하지 않는다

검은 배경 + 스켈레톤만. 디버그용 영상 토글도 넣지 않기로 했다. 사생활 보호를 눈으로 보여주는 것이 과제에서 가장 강한 포인트라, 토글이 있으면 그 메시지가 흐려진다.

### FCM 대신 폴링

FCM이 실제 제품에 맞지만, Firebase 프로젝트 생성 · google-services.json · 서버 키 · Play 서비스 있는 기기까지 깨질 지점이 너무 많다. 과제 시연에서 알림이 안 뜨면 프로젝트 전체가 실패로 보인다. 폴링은 외부 의존성이 0이고 에뮬레이터에서 바로 돈다. 대신 백그라운드 알림이 안 되는 한계를 README에 명시한다.

### Room 테이블을 만들지 않는다

방 등록 화면을 범위 밖으로 뒀으므로 Room 테이블이 있어도 시드 데이터로만 채워진다. 방 정보를 FallEvent에 직접 넣어 테이블 하나로 끝냈다. 방 관리 기능이 나중에 필요해지면 그때 분리한다.

### 목록 API가 폴링을 겸한다

`since` 파라미터를 두는 대신, 앱이 마지막으로 본 id를 로컬에 들고 응답의 최대 id와 비교한다. 엔드포인트가 하나로 줄고 목록 화면과 폴링이 같은 코드를 쓴다.

최초 로그인 시 기존 이벤트 전부에 알림이 쏟아지는 함정이 있다. 첫 응답의 최대 id는 알림 없이 저장하고 시작한다.

### 세 관문 (속도 → 자세 → 시간)

낙상 감지의 진짜 난제는 "넘어짐"과 "그냥 누움"의 구분이다. 단순 임계값(몸통이 수평이면 낙상)은 침대에 눕는 것도 전부 잡아서 시연 중 오탐지가 터진다.

- **속도** — 천천히 눕기를 거른다. 이게 1차 관문이라 가장 중요하다.
- **자세** — 급히 앉는 것을 거른다.
- **시간** — 스스로 일어난 사람 때문에 보호자를 깨우지 않는다.

학습 모델도 검토했으나 데이터셋 수집 · 라벨링 · 학습 · 변환이 과제 범위를 넘고, 데이터 없이는 규칙 기반보다 못하다.

### occurred_at은 FALLING 진입 시각

ALERTED 시각을 쓰면 보호자에게 실제보다 5초 늦은 시각이 표시된다. 판정은 나중에 확정되더라도 기록하는 시각은 실제로 넘어진 순간이어야 한다.

### detector.js를 순수 함수로 분리

MediaPipe · DOM · 네트워크를 하나도 안 쓰는 모듈로 뺐다. 웹캠 없이 가짜 랜드마크 시퀀스로 단위 테스트가 가능해진다. 브라우저는 같은 파일을 `<script type="module">`로 그대로 쓴다. Vitest는 dev 의존성일 뿐이고 배포 코드는 순수 JS다.

작업 순서에서 detector.js(2번)를 감지 페이지(3번)보다 먼저 두는 이유도 같다. 상태머신이 검증돼 있으면 3번에서 문제가 생겼을 때 원인이 랜드마크 품질임을 바로 안다.

### 낙상 POST 유실을 감수한다

재시도 3회 후 포기. 실제 제품이라면 절대 안 되는 동작이고 localStorage 큐잉이 맞지만, 과제 범위에서는 재시도 + 배너까지만 하고 README에 한계로 적기로 했다.

### 119는 더미 번호

시연 중 실제 발신되면 큰일이다. 상수를 더미로 두고 코드에 이유를 주석으로 남긴다.

---

## 2026-07-17 — 구현 계획 작성 중 검증한 것들

계획을 쓰기 전에 스크래치패드에서 실제로 돌려본 결과다. 계획의 코드 블록은 전부 아래 검증을 통과한 것이다.

### 툴체인 실측

Python 3.14.4 / Node 25.8.1 / Flutter 3.44.0 / Dart 3.12.0. `pip install django`는 **Django 6.0.7 + DRF 3.17.1**을 가져온다. Python 3.14에서 정상 동작하고 `authtoken` 마이그레이션도 통과한다. 설계 당시엔 버전을 못 박지 않았는데, 실측값을 계획에 고정했다.

### Django 6.0의 startproject 템플릿은 홑따옴표를 쓴다

`config/urls.py`가 `path('admin/', ...)`로 생성된다. 쌍따옴표를 가정하고 문자열 치환을 하면 **조용히 실패**하고 모든 라우트가 404가 된다. 스모크 테스트에서 실제로 이걸로 5개 테스트가 깨졌다. 계획에는 치환 대신 파일 전체 내용을 적었다.

### Android 에뮬레이터가 이 기기에 없다

`flutter emulators` → 없음. iOS 시뮬레이터(iPhone 17 Pro 등)만 있다. 설계 5절의 `10.0.2.2:8000`은 Android 전용 주소이고, iOS 시뮬레이터는 호스트 네트워크를 공유하므로 `127.0.0.1:8000`이다. 둘 다 살리려고 `api.dart`에서 `Platform.isAndroid` 분기 한 줄로 처리하기로 했다. 양쪽 플랫폼이 실제로 다 사정권에 있으므로 투기적 유연성이 아니다.

### resetMotion()은 방어선이 아니다 — 진짜 방어선은 Δt 계산이다

상태머신을 구현한 뒤 뮤테이션 테스트를 돌렸다. 속도 관문 제거 · `occurred_at`을 확정 시각으로 변경 · ALERTED 미전이는 전부 테스트를 깨뜨렸다(= 테스트에 이빨이 있다). 그런데 **`resetMotion()`을 통째로 지워도 7개가 전부 통과했다.**

이유를 파보니, `NO_PERSON`은 정의상 2초 이상 미검출일 때만 발동하므로 복귀 시점의 Δt가 항상 2000ms 이상이다. 좌표가 최대치(0.3)로 점프해도 가짜 속도는 `0.3 / 2.1s = 0.14/s`라 임계값 0.45에 한참 못 미친다.

설계 6절이 경고한 "재검출 순간의 좌표 점프가 거대한 가짜 속도로 잡힌다"는 **Δt를 고정 프레임 간격(33ms)으로 놓는 구현에서만** 터진다. 그 경우 `0.3 / 0.033 = 9.0/s`로 임계값의 20배다.

그래서 계획에서 강조하는 규칙은 `resetMotion()`이 아니라 **"Δt는 반드시 실제 타임스탬프 차이로 계산한다"**로 바꿨다. `resetMotion()`은 설계에 명시돼 있고 비용이 없으므로 심층 방어로 남기되, 이게 오탐지를 막는 근거라고 착각하지 않도록 주석과 계획에 적었다.

### MediaPipe CDN 버전

설계에는 버전이 없었다. `@mediapipe/tasks-vision`의 최신은 **0.10.35**이고, 아래 셋 다 200을 확인했다.

- `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/vision_bundle.mjs`
- `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/wasm`
- `https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task`

버전을 안 박고 `@latest`를 쓰면 브레이킹 체인지에 시연이 깨질 수 있어 고정했다.

### performance.now()를 그대로 occurred_at으로 보내면 안 된다

`detector.js`가 다루는 타임스탬프는 `performance.now()` 기준(페이지 로드 이후 경과 ms)이다. 이걸 그대로 서버에 보내면 1970년이 찍힌다. `new Date(performance.timeOrigin + occurredAt).toISOString()`으로 벽시계 시각으로 변환해야 한다. 계획 11번 태스크에 명시했다.

---

## 임계값 튜닝 기록

설계상의 초기값은 전부 추정치다. 실제 웹캠 테스트 후 조정한 내역을 여기에 남긴다.

측정 조건도 같이 적는다. 카메라 배치가 빠지면 아래 숫자는 재현할 수 없다.

- 카메라 배치 (높이 / 거리 / 각도): 사람보다 **높은 곳에서 내려다보는 각도**. 정확한 높이·거리·각도 수치는 미기록 — 재측정 시 반드시 채운다
- 측정일: 2026-07-17 (CSV `docs/tuning/tuning-2026-07-17T14-03-14-157Z.csv`, 20시행 8899프레임)

| 항목 | 초기값 | 조정값 | 이유 |
|---|---|---|---|
| FALL_VELOCITY | 0.45 /s | **유지 (실측 확인)** | 유효 구간 0.381~0.515의 한가운데가 0.448이다. 추정치가 우연히 최적값이었고 이제 근거가 생겼다 |
| TILT_UPRIGHT | 45° | 유지 | 앉기 tilt 최대 11°, 넘어지기 64~82°. 45°는 두 분포 사이 빈 공간에 있다 |
| TILT_FALLEN | 60° | 유지 | 넘어지기 5회 전부 통과(64~82°), 앉기 5회 전부 차단(≤11°). 여유가 양쪽 50°씩 |
| FALLING_WINDOW | 1000ms | 유지 | 넘어지기 5회 모두 진입 후 33~300ms 안에 수평 도달 |
| FALLEN_HOLD | 5000ms | **유지 (실측 검증)** | 3차에서 FALLEN 5.0초 유지 후 ALERTED 4회, POST 4건 DB 도달. 4.6초에 일어난 1회는 미전이 |
| EMA_ALPHA | 0.4 | 유지 | 서 있을 때 hipV 중앙값 0.005. 노이즈가 임계값을 묻지 않는다 |

### 2026-07-17 — 실측 미실시, 초기값 그대로 둔다

Task 16 진행 시점에서 실제 웹캠 앞에 서서 값을 재는 작업(Step 1~3)은 사람이 직접 해야 하는 수동 검증이라 이 세션에서는 수행할 수 없었다. 그래서 위 표는 채우지 못했고, `detector.js`의 `CONFIG`도 손대지 않았다 — 실측 없이 숫자를 바꾸는 것은 튜닝이 아니라 추측이기 때문이다.

즉 현재 `CONFIG` 값(FALL_VELOCITY 0.45/s, TILT_UPRIGHT 45°, TILT_FALLEN 60°, FALLING_WINDOW 1000ms, FALLEN_HOLD 5000ms, EMA_ALPHA 0.4)은 전부 설계 단계의 추정치이며, 실측으로 검증된 적이 없다. 실제 웹캠 앞에서 아래를 재기 전까지는 이 값들을 신뢰할 수 없다.

- 가만히 서 있을 때 `tilt` 범위와 `hipV` 노이즈 폭 — 이게 0선 근처에서 얼마나 튀는지 모르면 임계값이 노이즈에 묻히는지 알 수 없다.
- 침대에 천천히 눕는 동안의 `hipV` 최대치 — `FALL_VELOCITY`는 이 값보다 **커야** 한다.
- 의자에 급히 앉는 동안의 `hipV` 최대치와 `tilt` 최대치 — 앉는 동작이 속도 관문을 넘어와도 자세 관문(`TILT_FALLEN`)에서 걸러지는지 확인해야 한다.
- 빠르게 넘어질 때의 `hipV` 최대치 — `FALL_VELOCITY`는 이 값보다 **작아야** 감지가 된다.
- 바닥에 누워 있을 때의 `tilt` 범위 — `TILT_FALLEN`이 이 범위 안에 들어와야 FALLEN 상태가 유지된다.

핵심은 `FALL_VELOCITY`가 "천천히 눕기 최대치"와 "빠르게 넘어지기 최대치" 사이에 실제로 들어가는지다. 두 구간이 겹치면 속도만으로는 구분이 안 되는 것이고, 계획 문서(Step 1)가 지적한 대로 카메라 각도 문제일 가능성이 크다.

이 실측과 3회 연속 오탐지 없는 라이브 검증(계획 Step 3)은 이 프로젝트의 수동 검증 체크리스트로 넘긴다. `checklist.md`에도 해당 항목을 미완료로 남겨 뒀다.

---

## 2026-07-17 — 튜닝을 시작할 수 있게 측정 도구를 붙였다

실측을 하려고 절차를 다시 읽다가 계획서·체크리스트가 **실행 불가능한 절차를 적어놨다**는 것을 발견했다. "화면의 `tilt`/`hipV` 표시를 보며 잰다"고 되어 있는데, 그 표시는 30fps로 갱신되는 텍스트이고 빠른 낙상은 300ms(≈9프레임) 만에 끝난다. 눈으로 피크를 읽을 수 없고, 읽어야 할 순간에 측정자는 바닥에 누워 있다.

그래서 `web/js/tuning.js`를 추가했다. 시행별 피크를 화면에 **고정 표시**하고(일어난 뒤 읽는다), `r` 키로 시행을 끊고, 전 프레임 시계열을 CSV로 내보낸다. `detector.js`는 한 줄도 건드리지 않았다 — 측정 도구가 판정 로직에 스며들면 순수 모듈이라는 전제가 깨진다. 튜닝이 끝나면 이 파일과 호출부만 지우면 된다.

### 계획서의 판정 기준이 틀렸다

계획 Task 16 Step 1과 기존 체크리스트는 `FALL_VELOCITY`를 "천천히 눕기 최대치와 빠르게 넘어지기 **최대치** 사이"에 두라고 했다. 이건 틀렸다. 넘어지기를 5회 하면 피크가 5개인데, 임계값이 그중 최대치보다만 작으면 **가장 빠르게 넘어진 1회만 감지되고 나머지 4회는 놓친다.** 낙상 감지에서 미탐지는 오탐지보다 치명적이므로 그냥 넘어갈 오차가 아니다.

맞는 규칙은 `max(눕기·앉기 모든 시행의 피크) < FALL_VELOCITY < min(넘어지기 모든 시행의 피크)`다. 오탐지 쪽은 최댓값, 미탐지 쪽은 최솟값을 본다. 그래서 동작마다 5회씩 반복해야 하고 — 1회씩 재면 두 분포가 겹치는지 자체를 알 수 없다 — 체크리스트를 이 기준으로 고쳤다.

### 임계값은 카메라 배치와 한 세트다

`hipVelocity`는 정규화 좌표(0~1) 기준이라 몸이 프레임에서 차지하는 비율에 그대로 비례한다. 카메라를 2배 멀리 두면 같은 속도로 넘어져도 hipV가 절반이 된다. `0.45/s`는 물리 상수가 아니라 **특정 카메라 배치에서만 유효한 숫자**다. 그래서 튜닝 표에 카메라 높이·거리·각도 칸을 만들었다. 이게 없으면 다음 세션이 카메라를 옮기는 순간 튜닝이 무효가 되는데 아무도 그걸 모른다.

### tilt의 사각지대 — 실측으로 확인해야 한다

`detector.js`의 tilt는 `atan2(|dx|, |dy|)`로 이미지 평면에 투영된 어깨-엉덩이 벡터의 각도다. 카메라 **정면이나 정반대 방향으로** 넘어지면 몸통이 z축을 향해 투영 벡터가 짧아지고 dx가 0에 가까워진다. 즉 **바닥에 완전히 누워 있는데 tilt가 0°에 가깝게 나올 수 있고**, 그러면 2차 관문을 못 넘어 FALLEN에 도달하지 못한다.

아직 추론일 뿐이라 단정하지 않는다. 체크리스트에 "옆 / 정면 / 대각선 방향으로 누워 tilt 범위 측정"을 넣었다. 정면에서 60°를 못 넘기면 `TILT_FALLEN`을 낮춰서 해결할 일이 아니다 — 낮추면 앉기 오탐지가 터진다. 카메라를 옆에서 보는 각도로 옮기거나 README의 한계로 명시하는 게 맞다.

---

## 2026-07-17 — 실측 완료. 0.45는 유지하고, 구조적 결함을 하나 찾았다

20시행 8899프레임을 쟀다. 결론부터: **`CONFIG` 7개 값 전부 그대로 둔다.** 다만 그 이유가 "안 재봐서"에서 "재보니 맞아서"로 바뀌었다.

### 시행 전체의 피크를 보면 안 된다

처음엔 시행별 hipV 피크를 뽑아 비교했는데 눕기 최대 1.058, 넘어지기 최소 0.725로 **구간이 완전히 겹쳤다.** 속도로는 못 가른다는 결론이 나올 뻔했다.

틀린 건 데이터가 아니라 측정 창이었다. 각 시행에는 침대까지 걸어가는 동작이 섞여 있고 피크는 그걸 잡았다. 상태머신은 그 창을 보지 않는다 — `STANDING`에서 hipV가 임계값을 넘어야 `FALLING`이고, 거기서 1초 안에 tilt가 60°를 넘어야 `FALLEN`이다. 그러니 **tilt가 60°를 넘기 직전 1초**의 hipV만이 관문 1이 실제로 보는 값이다. 그 창으로 다시 재니 눕기 0.052~0.194, 넘어지기 0.515~0.641로 깨끗하게 갈렸다.

교훈은 임계값이 아니라 방법론이다. 지표를 잴 때는 **판정 코드가 그 지표를 읽는 순간과 같은 창**에서 재야 한다. 아니면 무관한 동작의 노이즈를 신호로 착각한다.

### hipV는 실제 속도가 아니라 겉보기 속도다

가만히 서 있는 시행에서 hipV가 0.845까지 튀었는데 그 구간 내내 **tilt는 0.5°에서 꿈쩍도 안 했다.** 몸은 완벽히 수직인데 엉덩이만 빠르게 내려간 것으로 측정됐다. 카메라 쪽으로 걸어간 것이다.

좌표가 정규화(0~1)돼 있으니 카메라에 다가가면 몸이 프레임에서 커지고, 수직으로 걷기만 해도 엉덩이의 정규화 y가 빠르게 변한다. **hipV는 물리적 낙하 속도가 아니라 화면상 겉보기 속도를 잰다.** 그래서 카메라에 다가가면 화면에 `FALLING`이 뜬다.

이건 버그가 아니다. 관문 2가 막는다 — 걸어갈 때 tilt는 0°라 `FALLING`은 1초 뒤 만료되고 `FALLEN`에 도달하지 못한다. 실측에서도 서 있기·앉기 11시행 중 `FALLEN` 도달은 0회였다. `FALLING`은 중간 상태이고 스쳐 지나가는 게 정상이다. 화면에 그게 보이는 게 불안하면 UI 문제이지 판정 문제가 아니다.

### `STANDING`이라는 이름은 거짓말이다 — 구조적 결함

`FALL_VELOCITY`를 0.35로 내려보니 천천히 눕기 5회 중 2회가 `FALLEN`에 도달했다. 추적해보니 이랬다.

```
시행3  FALLING 진입  hipV=0.356  이때 tilt=73.0°   ← 이미 누워 있다
시행3  FALLEN — 진입 33ms 만에
```

천천히 누우면 관문 1을 통과 못 하므로 상태가 `STANDING`에 남는다. **그런데 몸은 이미 수평이다.** `STANDING`의 실제 의미는 "서 있음"이 아니라 "낙하 중이 아님"이고, 여기에 "평화롭게 누워 있음"이 포함된다. 이 상태에서 hipV가 한 번 튀면 `FALLING`으로 가고, tilt가 이미 60°를 넘으므로 **다음 프레임에 즉시 `FALLEN`**이다. 관문 1과 2가 동시에 열려 있어 사실상 관문이 없다.

침대에서 뒤척이다 5초간 안 일어나면 보호자 폰이 울린다는 뜻이다.

실측 데이터에서 **이미 수평인 상태의 hipV 최대치는 0.381**이었다(대각선으로 누운 시행). 그래서 0.45에서는 아직 안 뚫린다. 여유는 0.069뿐이다.

### 그래서 0.45다

세 제약이 걸린다.

- `> 0.381` — 이미 수평인 상태의 튐. 위 결함 경로를 피한다
- `> 0.194` — 천천히 눕기의 관문 창 최대치
- `< 0.515` — 넘어지기 5회의 최솟값. 이 위로 올리면 낙상을 놓친다

유효 구간 **0.381~0.515, 한가운데 0.448**. 설계 단계 추정치 0.45가 우연히 최적값이었다. 임계값을 스윕해 상태머신을 CSV로 재생한 결과도 0.40~0.50에서만 넘어지기 5/5·오탐지 0을 만족했다.

### 고치지 않기로 한 것

관문 1에 `tilt < TILT_UPRIGHT`를 추가하면 위 결함이 닫힌다. 재생해보니 안전 구간이 0.40~0.50에서 0.35~0.50으로 넓어졌다. 그런데 **넣지 않았다.**

허리를 굽힌 자세(물건을 줍다가, 어지러워서)에서 쓰러지면 tilt가 이미 45°를 넘어 있다. 그러면 관문 1이 아예 안 열려 **낙상을 통째로 놓친다.** 노인은 자주 굽힌다. 침대 뒤척임 오탐지(현재 여유 0.069)와 굽힌 자세 낙상 미탐지를 맞바꾸는 것인데, 이 프로젝트는 미탐지를 더 무겁게 본다. 실측이 0.45에 여유가 있다고 말하고 있으므로 지금은 구조를 건드리지 않는 쪽이 맞다.

카메라를 옮기거나 사람이 바뀌어 수평 상태의 튐이 0.45를 넘기 시작하면 이 결정을 다시 봐야 한다. 그때는 임계값을 올리지 말고(0.515 천장에 부딪힌다) `LYING` 상태를 따로 두는 쪽을 검토한다.

### tilt 사각지대는 이 배치에서 발생하지 않는다

옆·정면·대각선 세 방향 모두 tilt가 60°를 넘었다(88°, 89°, 88°). 사각지대는 **몸통 축이 카메라 광축과 일직선**일 때 생기는데, 카메라가 사람보다 높은 곳에서 내려다보면 바닥에 누운 몸은 항상 이미지 평면에 펼쳐져 투영이 짧아지지 않는다.

즉 **높은 각도는 취향이 아니라 tilt를 의미 있게 만드는 전제조건이다.** 카메라를 바닥 높이로 내리면 사각지대가 살아난다. 옮길 때 반드시 세 방향을 다시 잰다.

### 아직 검증 안 된 것

`FALLEN_HOLD`(5000ms)는 못 쟀다. 넘어지기 5회 모두 `FALLEN`까지는 갔지만 5초 전에 일어나서 `ALERTED`에 도달한 시행이 없다. 5초 유지 → 전송까지의 경로는 여전히 Vitest에서만 검증됐다.

---

## 2026-07-17 (2차) — 카메라를 옮겼더니 30분 만에 튜닝이 무효가 됐다

1차 측정 직후 `FALLEN_HOLD` 5초를 검증하려고 다시 쟀다(`docs/tuning/tuning-2026-07-17T14-31-24-149Z.csv`). **넘어지기 5초 유지 3회, 알림 0회.** 100% 미탐지다.

원인을 파보니 임계값이 아니라 카메라였다. 두 세션 사이에 노트북을 다시 올려놓았고, "같은 높이"였지만 각도가 바뀌었다.

### 화면상 하강량이 0이었다

낙상 1회 동안 엉덩이가 화면에서 내려간 총량(hipV 적분):

| | 하강량 (정규화 단위) |
|---|---|
| 1차 낙상 5회 | +0.233 ~ +0.284 |
| 2차 낙상 3회 | **-0.029 ~ +0.032** |

같은 사람이 같은 높이에서 넘어지면 엉덩이는 물리적으로 70cm쯤 떨어진다. 1차 카메라는 그걸 봤고 2차 카메라는 못 봤다. **위에서 수직으로 내려다보면 낙하 방향이 카메라 광축과 나란해져서, 엉덩이가 아무리 떨어져도 화면에서는 제자리다.**

파형을 보면 더 분명하다. 1차는 hipV가 0.587까지 치솟는 동안 tilt가 아직 3~9°다(엉덩이가 먼저 떨어지고 몸이 회전한다). 2차는 tilt가 0.8초 만에 7.7°→78.8°로 확 도는데 hipV는 내내 0 근처다(회전만 하고 하강이 없다). **동작은 빨랐다. 카메라가 못 본 것이다.**

### 2차 배치에서는 유효한 임계값이 존재하지 않는다

2차 낙상 3회의 관문 창 hipV는 0.066 / 0.154 / 0.177이다. 1차의 **천천히 눕기**(0.052~0.194)와 완전히 겹친다. 가를 숫자가 없다. 임계값을 아무리 내려도 낙상과 눕기가 같은 값이므로 오탐지 아니면 미탐지다.

### 카메라 각도에는 유효 대역이 있고 양쪽 끝이 다 조용히 실패한다

이번에 알게 된 것이다. 두 실패 모드가 반대 방향에 있다.

- **너무 수평** → tilt 사각지대. 몸통 축이 광축과 일직선이 되면 투영이 짧아져 바닥에 누워도 tilt가 0°에 가깝다. 관문 2를 못 넘는다.
- **너무 수직(내려다봄)** → hipV 소실. 수직 하강이 광축 방향이라 화면에서 안 움직인다. 관문 1을 못 넘는다. **← 2차가 여기 빠졌다**

1차 배치는 이 대역 안에 있었다. 그런데 **1차 배치의 수치를 안 적어놨다.** 정확히 이걸 경고해놓고 당했다.

### 배치 검수 절차 — 수치 대신 합격 기준으로 정의한다

높이·거리·각도 수치를 복원할 수 없으므로, 배치가 유효한지 **측정으로 판정**하는 기준을 둔다. 카메라를 옮길 때마다 둘 다 통과해야 한다.

- [ ] 빠르게 넘어진다 → 튜닝 패널의 **hipV 피크가 0.5 이상**. 못 넘으면 너무 위에서 내려다보는 것이다. 1차 실측 낙상 5회는 0.515~0.641이었다
- [ ] 옆·정면·대각선으로 누워본다 → **tilt가 셋 다 60° 이상**. 못 넘는 방향이 있으면 너무 수평이다. 1차는 셋 다 88° 이상이었다

이 두 기준은 각각 관문 1과 관문 2가 살아 있는지를 직접 확인한다. 수치를 적는 것보다 이쪽이 낫다 — 방이 바뀌어도 그대로 쓸 수 있다.

### 기록: 실패한 배치

높이 177cm / 거리 170cm / 각도 68° → **불합격.** 낙상 3/3 미탐지, 하강량 ≈ 0. (각도 68°가 무엇을 기준으로 한 값인지는 불명확하다. 수평 기준이라면 거의 내려다보는 각도이고 위 실패와 맞아떨어진다.)

`CONFIG`는 손대지 않았다. 2차 데이터는 임계값 근거로 쓸 수 없다 — 배치가 깨진 상태의 측정이라 여기서 나온 숫자로 임계값을 정하면 깨진 배치에 맞춰 튜닝하는 셈이다. 1차 데이터가 유일하게 유효하고, 거기서 나온 결론(0.45 유지)은 그대로다. 단 **1차 배치를 복원해야 그 결론이 살아난다.**

---

## 2026-07-17 (3차) — FALLEN_HOLD 5초 경로가 처음으로 실측 검증됐다

2차 배치를 버리고 카메라를 옆에서 보는 각도로 낮춰(높이 150cm / 거리 180cm / 각도 83°) 다시 쟀다(`docs/tuning/tuning-2026-07-17T15-06-06-094Z.csv`). 빠르게 5회 넘어지고 앞 2회는 10초 버텼다. **배치가 유효 대역 안에 들어왔고, 두 세션 동안 막혀 있던 5초 알림 경로가 실제로 작동했다.**

시행을 `r`로 안 끊어 전부 take 1(3116프레임, 104초)에 들어 있어서, 시행별 피크 대신 `state` 열의 전이 타임라인으로 판정했다. 오히려 더 강한 증거다 — 검출기가 실제로 어떤 상태로 갔는지가 그대로 찍혀 있다.

### 낙상 5회 전부 감지, 알림 4회

FALLING→FALLEN까지 간 게 정확히 5회다. FALLEN 진입 시 tilt는 60·67·64·69·63°로 전부 관문 2를 넘었다. 그중 4회가 FALLEN에서 **정확히 5.0초** 머문 뒤 ALERTED로 넘어갔다(앞 2회는 10초, 뒤 2회도 5초를 넘겨 버팀). 3번째(147.3s)만 **4.6초 만에 일어나** ALERTED에 못 갔다 — "5초 안에 일어나면 알림 없음"이 그 경계에서 정확히 작동한 것이다.

### POST 4건이 DB까지 도달했다 — 종단 검증

ALERTED 4회가 POST 4건이 되어 DB에 남았다(FallEvent id 2~5, 전부 "안방 1"). 결정적으로 `occurred_at` 간격이 CSV의 FALLING 진입 간격과 초 단위로 일치한다.

| DB id | occurred_at | 다음까지 | CSV FALLING 간격 |
|---|---|---|---|
| 2 | 15:04:39.984 | 22.5s | 22.6s |
| 3 | 15:05:02.517 | 39.0s | 39.0s |
| 4 | 15:05:41.551 | 9.7s | 9.7s |
| 5 | 15:05:51.251 | — | — |

세 가지를 한 번에 증명한다. 브라우저→Django→SQLite 파이프라인이 살아 있다. `occurred_at`이 확정 시각(ALERTED)이 아니라 넘어진 순간(FALLING)으로 기록된다 — 설계 의도 그대로다. 그리고 Vitest로만 검증됐던 `FALLEN_HOLD` 5초가 실측으로 확정됐다. 벽시계 시각(2026-07-17 15시)도 정상이라 `performance.timeOrigin` 변환도 함께 확인됐다.

### 두 관문 필터가 그대로 작동했다

FALLING 진입은 14회였지만 FALLEN까지 간 건 5회뿐이다. 나머지 9회는 전부 tilt 0~5°의 **서 있는 상태에서 빠르게 움직인 것**(카메라로 걸어가기, 일어서기, 마지막에 다운로드 누르러 가기)이고 hipV가 최대 2.995까지 튀었지만 관문 2가 전부 막았다. 겉보기 속도(hipV)가 아무리 커도 몸이 수직이면 안 넘어간다 — 1차에서 추론한 걸 다시 확인했다.

### 남은 구멍 — 이 배치의 오탐지 쪽을 안 쟀다

이번엔 넘어지기만 5회 했다. 눕기·앉기를 이 배치에서 안 재서 **오탐지 천장을 모른다.** 게다가 넘어지기 관문 창 hipV 피크가 0.456·0.513·0.519·0.498·0.540으로, **최솟값 0.456이 임계값 0.45를 겨우 0.006 넘는다.** 1차(0.515~0.641)보다 낮다 — 거리가 170→180cm로 멀어져 몸이 프레임에서 작아졌고 정규화 속도가 그만큼 줄었다.

즉 이 배치는 **낙상 감지와 5초 알림은 확실히 되지만, 0.45가 이 자리에서 잘 가운데 있는지는 아직 모른다.** 두 갈래로 마무리할 수 있다.

- 이 배치를 유지하고 **천천히 눕기 5회 + 급히 앉기 5회**를 여기서 재서 오탐지 천장을 확인한다. 그게 0.45보다 충분히 낮으면 끝이다. 낙상 쪽 여유가 얇으니 오히려 `FALL_VELOCITY`를 이 배치에 맞춰 조금 낮추는 게 나을 수도 있는데, 눕기 데이터가 있어야 안전하게 정한다.
- 카메라를 조금 더 가까이(거리 ~165cm) 당겨 낙상 hipV 피크를 1차 수준으로 올려 여유를 복원한다. 대신 배치가 또 바뀌므로 낙상 5회를 다시 확인해야 한다.

`CONFIG`는 이번에도 손대지 않았다. 이 배치에서 5회 다 감지됐으니 0.45를 내릴 근거는 아직 없고, 눕기 데이터 없이 내리면 오탐지를 부를 수 있다.

### 기록: 성공한 배치

높이 150cm / 거리 180cm / 각도 83° → **합격 (단 2-체크 기준. 4차에서 ③을 추가하니 불합격 — 아래 참조).** 낙상 5/5 감지, FALLEN tilt 60~69°(최대 89°), 알림 4/4 DB 도달. 2차 실패 배치(177/170/68)와 비교하면 높이를 낮추고 거리를 늘려 시야가 평평해졌다. 사용자 각도 표기로는 68°→83°로 커졌는데, 이 표기에서는 큰 값이 덜 내려다보는 쪽이다.

---

## 2026-07-17 (4차) — 오탐지 쪽을 재보니 이 배치는 못 쓴다. 유효 대역이 양쪽에서 좁혀졌다

3차에서 남긴 구멍(이 배치의 오탐지 쪽 미측정)을 메우려고 천천히 눕기 5회 + 급히 앉기 5회를 같은 배치(150/180/83)에서 쟀다(`docs/tuning/tuning-2026-07-17T15-34-06-522Z.csv`). **실제 오작동은 없었다(ALERTED 0회). 그런데 재보니 이 배치는 쓰면 안 된다.**

### 눕기·앉기가 전부 관문 1을 넘었다

FALLING 진입이 9회, 전부 눕기·앉기다. 관문 창 hipV가 0.456·0.474·0.477·0.510·0.513·0.531·0.541·0.623이다. 3차 낙상의 최솟값 0.456과 **완전히 겹친다.** 이 배치에서 hipV(관문 1)는 눕기와 넘어지기를 전혀 못 가른다.

앉기가 관문 1을 넘는 건 원래 괜찮다 — 앉으면 tilt가 안 올라가 관문 2가 막는다(1차에서 확인). 문제는 **눕기**다.

### 5° 차이로 오탐지를 비켜갔다

43.4초 눕기는 FALLING 상태에서 **tilt가 55°까지** 올라갔다. 60°였으면 FALLEN이고 5초 뒤 오탐지였다. FALLING 창(1초)이 tilt 55°에서 만료돼 살았다. 여유가 아니라 타이밍이 우연히 막은 것이다.

### 속도 문제가 아니라 배치 문제다

눕기 5회를 하강 구간(tilt 15°→65°)에서 다시 재니:

| 눕기 시작 | 소요 | 하강 hipV 피크 | 관문 1 |
|---|---|---|---|
| 6.5s | 1.5s | 0.330 | 안 넘음 |
| 19.5s | 2.1s | 0.477 | **넘음** |
| 33.5s | 0.8s | 0.224 | 안 넘음 |
| 43.4s | 1.6s | 0.541 | **넘음** |
| 55.8s | 0.6s | 0.209 | 안 넘음 |

넘긴 두 눕기가 오히려 **더 느렸다**(2.1s·1.6s vs 안 넘긴 것 0.6~1.5s). 빨리 누워서 넘은 게 아니다. 눕기 hipV 분포 자체가 0.209~0.541로 1차의 0.052~0.194보다 통째로 위로 올라갔다. 카메라를 옆에서 보는 각도로 옮기면 눕는 동작이 화면을 크게 쓸고 지나가 hipV가 커진다. **낙상엔 좋은 변화가 눕기엔 나쁜 변화다.**

### 유효 대역은 양쪽 끝이 다 hipV 겹침이다 — 같은 증상, 반대 원인

2차와 3차가 같은 병(눕기·낙상 hipV 겹침, 유효 임계값 없음)인데 원인이 반대다.

- **2차 (너무 내려다봄, 177/170/68)** — 낙상 hipV가 눕기 수준으로 **주저앉았다**(하강이 광축 방향이라 안 보임). 낙상을 놓친다.
- **1차 (스위트스팟)** — 낙상 0.515~0.641, 눕기 0.052~0.194. 0.32 간격. 0.45가 그 사이에 있다.
- **3차 (너무 옆에서 봄, 150/180/83)** — 눕기 hipV가 낙상 수준으로 **치솟았다**(눕는 동작이 화면을 쓸어서). 오탐지가 난다.

카메라는 눕기를 짧게 보일 만큼 위에 있어야 하고(눕기 hipV 억제), 동시에 낙하를 볼 만큼 옆이어야 한다(낙상 hipV 확보). 이 둘을 동시에 만족하는 각도 대역이 생각보다 좁다. 1차가 거기 있었고 2·3차는 양옆으로 벗어났다.

### 2-체크 검수 기준은 부족했다 — 3번째를 추가한다

2차 뒤에 만든 검수 기준(① 낙상 hipV ≥ 0.5 ② 눕기 tilt ≥ 60°)을 **3차 배치는 둘 다 통과하고도 위험했다.** 낙상은 0.5를 넘겼고 눕기 tilt는 89°까지 갔다. 빠진 건 세 번째다.

- ③ **천천히 눕기의 hipV 피크가 낙상보다 확실히 낮을 것.** 눕기 < 0.3, 낙상 > 0.5처럼 0.2 이상 벌어져야 한다. ①②는 각 관문이 열리는지만 보고, 둘을 **가를 수 있는지**는 ③만 본다.

3차는 ③에서 떨어진다(눕기 0.541, 낙상 0.456, 겹침).

### CONFIG는 이번에도 그대로

이 배치엔 맞는 임계값이 없으니 0.45를 딴 값으로 바꿔도 소용없다 — 눕기 0.541 위로 올리면 낙상 0.456을 놓친다. 0.45는 1차 스위트스팟에서 옳은 값이고, 고칠 건 임계값이 아니라 배치다. `detector.js` 무변경.

분석은 `scripts/state-timeline.mjs`(state 전이 타임라인)와 `scripts/lie-events.mjs`(눕기 구간별 하강 hipV)로 했다. 튜닝이 끝나면 `analyze-tuning.mjs`와 함께 지워도 된다.

## 실기기 알림 검증 (2026-07-18)

미뤄뒀던 **보호자 폰 알림 수신**을 실제 iPhone(iOS 26.5)에서 검증했다. 시뮬레이터가 아니라 실기기라 시뮬레이터용 설정 몇 개를 실기기용으로 바꿔야 했다.

### 실기기 배포에 필요했던 변경 4개

- `app/lib/api.dart` — 실기기는 `127.0.0.1`로 Mac에 못 닿는다. `--dart-define=API_HOST=<Mac IP>`로 서버 주소를 덮어쓰게 했다(플랫폼 분기는 기본값으로 유지). 실행은 `flutter run -d <기기> --dart-define=API_HOST=<Mac IP>`.
- `app/ios/Runner/Info.plist` — iOS ATS가 평문 HTTP를 막으므로 `NSAllowsLocalNetworking`으로 사설망 접속을 허용했다. `NSLocalNetworkUsageDescription`도 넣었지만, iOS 26에선 사설 IP 직접 접속에 로컬 네트워크 권한이 강제되지 않아 이 키 없이도 붙었다(현재 실행 빌드엔 이 키가 없다). 앱이 실제로 로컬망에 접속하므로 선언상 맞아 남겨둔다.
- `app/lib/notifications.dart` — iOS는 앱이 **포그라운드**일 때 기본적으로 배너를 안 띄운다. poller가 포그라운드에서 도니까 `DarwinNotificationDetails`의 `presentAlert/presentBadge/presentSound`를 켰다. 안 켜면 앱을 보고 있을 때 알림이 조용히 삼켜진다.
- `backend/config/settings.py` — `ALLOWED_HOSTS`를 DEBUG 중 개방(`['*'] if DEBUG else []`). 서버는 `runserver 0.0.0.0:8000`으로 전체 인터페이스에 바인딩해야 폰이 붙는다.

서명 팀(`DEVELOPMENT_TEAM`)은 Xcode에서 개인 Apple ID로 설정했다. 개인 식별자라 `project.pbxproj`의 이 변경은 커밋하지 않고 로컬에만 둔다.

### 막혔던 두 지점 — 로그로 원인 확정

1. **폰이 서버에 아예 못 닿음** (`SocketException ... errno 48`). Django 로그에 폰 요청이 안 찍혀서 "요청 미도달"로 좁혔다. 원인은 폰이 Mac과 **다른 Wi‑Fi**였던 것이다. 같은 Wi‑Fi로 옮기니 요청이 도달했다. 방화벽·IP·바인딩은 모두 정상이었다.
2. **로그인 400** — 도달 후엔 자격 증명 거부였다. 로그에 `POST /api/auth/login/ 400` 다섯 번. `guardian` 비밀번호를 `guardian1234`로 재설정하니 `200` + 토큰이 나왔다. 시연·다음 세션은 이 계정으로 로그인한다.

### 결과 — 알림 5초 내 도달

로그인 후 poller가 기준선(최대 id 6)을 잡은 뒤 `manage.py`로 낙상 1건(id 7, 안방 1)을 주입했다. 서버 로그에서 주입 직후 폴링(16:32:03)의 응답이 1105→1279바이트로 커졌고(새 이벤트 수신), 폰에 "안방 1에서 낙상 감지" 배너가 떴다. 3차에서 검증한 "웹캠 낙상 → POST → DB"와 합치면 **넘어짐부터 보호자 알림까지 전 구간이 두 검증으로 이어진다.** 주입 검증이라, 마지막으로 "실제 웹캠 낙상 → 폰 알림"을 한 번에 보이려면 웹 감지 페이지를 함께 띄우면 된다.

## 배포 준비 (2026-07-18)

"배포하지 않는다" 전제를 뒤집고 실서비스 배포(백엔드 Render, 감지 페이지 GitHub Pages, 앱 실기기)를 준비했다. 절차 문서는 docs/DEPLOYMENT.md.

- **Render 감지는 `RENDER` 환경변수로** — Render가 자동 주입하는 변수라 별도 설정이 필요 없다(공식 Django 가이드 패턴). 로컬은 지금까지처럼 DEBUG=True로 동작하고, 기존 개발 흐름(LAN 실기기 포함)은 그대로다.
- **배포 DB는 Postgres 강제** — Render 무료 웹 서비스의 디스크는 재배포·슬립 때마다 초기화되므로 SQLite면 보호자 계정과 낙상 기록이 계속 사라진다. `dj-database-url`로 `DATABASE_URL` 있으면 Postgres, 없으면(로컬) SQLite. 무료 Postgres는 생성 30일 뒤 만료 — 대안(Neon 등)은 DATABASE_URL 교체만으로 갈아탈 수 있게 했다.
- **migrate·createsuperuser는 startCommand에서** — 무료 플랜엔 preDeploy가 없고, 빌드 시점의 DB 접근은 보장이 불확실하다. 시작 시점은 항상 DB에 닿는다. `createsuperuser --noinput || true`로 멱등(이미 있으면 통과). 셸 접근이 없는 무료 플랜에서 보호자 계정을 만들 유일한 통로가 DJANGO_SUPERUSER_* 환경변수다.
- **정적 파일은 whitenoise, manifest 스토리지는 안 씀** — admin 화면용이 전부라 압축·해시 스토리지의 엄격성(누락 파일에 collectstatic 실패)이 이득보다 리스크다. `WHITENOISE_AUTOREFRESH = DEBUG`로 개발 중 staticfiles 미존재 경고 제거.
- **웹 주소 분기는 location.hostname으로** — 127.0.0.1/localhost면 로컬 Django, 그 외(Pages)면 PROD_API_BASE. 빌드 도구가 없는 순수 정적 페이지라 환경변수 주입이 불가능해 런타임 분기를 택했다.
- **앱 주소 분기는 kReleaseMode로** — 우선순위는 API_HOST(dart-define, LAN 개발) > 릴리즈=배포 주소 > 디버그=로컬. 릴리즈로 폰에 설치하면 자동으로 Render를 본다.
- **배포 주소는 자리 표시자(XXXX)** — Render URL엔 랜덤 접미사가 붙어 배포 전엔 알 수 없다. 배포 후 web/js/api.js·app/lib/api.dart 두 곳을 실제 주소로 교체하는 단계가 DEPLOYMENT.md 3단계다. 잘못된 주소로 조용히 남의 서버를 치는 것보다 명시적으로 실패하는 자리 표시자가 낫다.
- **Pages는 Actions 방식** — web/을 docs/로 복제하거나 gh-pages 브랜치를 만들지 않고 워크플로가 web/ 디렉토리만 아티팩트로 올린다. 저장소가 public이어야 무료 Pages가 된다.
- **CORS는 환경변수로 확장** — 코드에 GitHub 아이디를 하드코딩하지 않도록 `CORS_ALLOWED_ORIGINS`(쉼표 구분)를 기존 로컬 5500 목록에 더한다. 값 형식 주의(끝 슬래시·경로 금지) — 문제 해결 표에 넣었다.
- **Android 릴리즈 매니페스트에 INTERNET 권한 추가** — 디버그는 Flutter 도구가 자동 주입하지만 릴리즈는 없으면 네트워크가 그냥 죽는 고전 함정. 지금 폰은 아이폰이지만 한 줄이라 미리 막았다.
- **검증** — pytest 7·vitest 10·flutter test 4 전부 통과, flutter analyze 클린. Render 환경 시뮬레이션(RENDER=1 + 환경변수)으로 check --deploy, collectstatic 157개, wsgi 로드, 헬스체크 200, Pages 오리진 CORS 허용·타 오리진 차단을 확인. iOS 릴리즈 빌드(--no-codesign) 컴파일 성공. check --deploy 잔여 경고 3건은 의도적 — HSTS·SSL 리다이렉트는 Render 엣지가 처리하고, SECRET_KEY 경고는 테스트용 짧은 키 탓(실배포는 generateValue).
- **혼동 주의** — 시연·과제 맥락 문구(README 첫 줄 등)는 "배포 지원"으로 갱신했지만, 알려진 한계(백그라운드 알림 불가, 전송 유실 등)는 배포 후에도 그대로 유효하다. 특히 "앱이 떠 있어야 알림이 온다"는 운영 안내를 DEPLOYMENT.md 6절에 명시했다.

---

## 발표자료 pre.html (2026-07-18)

과제 발표용 슬라이드를 `pre.html` 단일 파일로 만들었다. 구성은 제주 노인 문제(기사) → 낙상 통계 → 사생활 딜레마 → MediaPipe 해법 → 구조 → 세 관문 원리 → 실측 검증 → 한계 → 출처, 총 14장.

### 기사 캡쳐는 헤드리스 Chrome으로 직접 찍었다

제주의소리(고령화·고독사), 제주일보, 헤드라인제주, 부산일보(낙상) 5건을 `--headless=new --screenshot`으로 캡쳐해 base64로 인라인했다. 파일 하나만 옮기면 발표가 되도록 외부 의존성(이미지 파일·CDN·폰트)을 전부 없앴다. 결과물 1.4MB. 부산일보는 상단 광고 여백이 커서 sips `--cropOffset`으로 헤드라인~통계 차트 구간만 잘랐다. 저작권 주석과 출처 목록(마지막 장)을 넣었다.

### 발표에 쓴 수치는 두 곳에서만 가져왔다

프로젝트 쪽 수치(hipV 분포, tilt, 알림 4/4, 5초 등)는 이 파일의 실측 기록에서, 사회 통계는 캡쳐한 기사 원문과 KDI 나라경제(추락 사망 1,802명, 2021년부터 사고 사망 1위)에서 가져왔고 WebFetch로 원문을 재확인했다. 고령화 추이 선그래프는 출처 혼합을 피하려고 제주의소리(통계청 재인용) 한 곳의 시계열만 썼다 — 제주일보의 내국인 기준 수치와 섞으면 기준이 달라진다.

### 다크 단일 테마로 못 박은 이유

이 프로젝트의 시각 정체성이 "검은 배경 위 스켈레톤"이다. 발표 화면도 같은 톤이어야 7번 슬라이드(카메라가 보는 것 vs 화면에 표시되는 것)가 설득력을 가진다. 차트 색은 dataviz 스킬 절차대로 검증했다 — 파랑 #3987e5 + 비강조 회색 #6b6a66 (CVD ΔE 19.1, 다크 표면 대비 3:1 이상, validate_palette.js 통과). 고독사·hipV·tilt 차트는 전부 강조(emphasis) 형태이고 모든 값을 직접 라벨링해서 호버 없이도 읽힌다.

### 렌더링은 눈으로 확인했다

작성 후 헤드리스 Chrome으로 14장 전부 캡쳐해서 봤다. 4건을 고쳤다 — 부산일보 캡쳐 하단 백지·광고 노출(이미지 크롭으로 해결), 10·11장 카드가 화면 높이만큼 늘어나는 문제(row에서 fill 제거 후 세로 중앙 정렬), 13장 각도 다이어그램 텍스트 잘림(viewBox 확장 + 라벨 이동), 14장 출처 카드 하단 공백(세로 중앙 정렬). 슬라이드는 ←/→·스페이스 이동, F 전체화면, `#n` 해시 진입, 인쇄 시 전 장 순차 출력을 지원한다.

## 회원가입 추가 (2026-07-18)

계정 생성이 createsuperuser뿐이라는 한계를 걷어내고 `POST /api/auth/signup/`을 추가했다. TDD(테스트 3개 선작성 → 404 확인 → 구현 → 10개 통과).

- **가입 응답이 곧 로그인** — 201과 함께 토큰을 돌려준다. 웹·앱 모두 가입 성공 시 토큰을 저장하고 바로 다음 화면으로 간다. 가입 직후 로그인 화면으로 돌려보내는 UX는 같은 정보를 두 번 입력시키는 것이라 뺐다.
- **비밀번호 규칙은 기존 AUTH_PASSWORD_VALIDATORS 재사용** — 별도 규칙을 만들지 않았다. serializer의 validate_password가 settings 검증기를 그대로 호출한다. 규칙을 바꾸려면 settings만 고치면 된다.
- **LANGUAGE_CODE를 ko로** — 중복 아이디·약한 비밀번호 에러가 Django 내장 한국어 번역으로 내려온다("해당 사용자 이름은 이미 존재합니다." 실측). 커스텀 메시지 매핑 없이 공짜로 얻는 한국어 UX라 채택했다. 부수 효과로 admin도 한국어가 된다. TIME_ZONE은 건드리지 않았다(API 시각 표현이 바뀌므로).
- **에러 표시 규약** — DRF 검증 에러는 {필드: [메시지...]} 형태다. 웹·앱 모두 첫 메시지 하나만 표시한다(Object.values(...).flat()[0] / body.values.first). 필드별 표시는 화면 복잡도 대비 이득이 없어 뺐다.
- **공개 가입의 보안 범위** — 가입이 열려 있어도 낙상 데이터는 guardian=요청자 필터로 계정별 격리되어 남의 데이터에 닿을 수 없다(기존 테스트가 보장). 남는 리스크는 대량 계정 생성(스팸)인데, 레이트리밋은 범위 밖에 뒀다 — README 한계에 명시.
- **superuser의 역할 축소** — 이제 admin 접속용이다. DEPLOYMENT.md 2단계 문구를 관리 계정으로 고쳤고, 보호자는 회원가입으로 만든다.
- **검증** — 실서버 E2E(임시 DATABASE_URL sqlite)로 가입→토큰→목록 200, 중복 400 한국어 메시지 확인. flutter analyze 클린, 3종 테스트(10·10·4) 전부 통과.

## 브랜치 배포 안전망 + HTML 가이드 (2026-07-18)

사용자가 Pages를 "Branch: main"(브랜치 배포) 방식으로 설정할 계획이라고 알려왔다. 그 모드는 저장소 루트가 사이트 루트가 되므로, 루트에 index.html이 없던 기존 상태로는 첫 화면에 README가 렌더링됐을 것이다.

- **루트 index.html 리다이렉트 추가** — meta refresh + location.replace 이중으로 web/으로 보낸다. 이제 Source=GitHub Actions(권장, web/이 곧 사이트 루트)와 브랜치 배포(루트에서 한 번 이동) 어느 쪽을 선택해도 URL 클릭 → 로그인 화면이 된다.
- **두 모드는 충돌하지 않는다** — Actions 모드의 아티팩트는 web/ 내용물뿐이라 루트 index.html이 배포에 포함되지 않는다.
- **.nojekyll 추가** — 브랜치 배포는 Jekyll 빌드를 거치므로, 예측 불가능한 빌드 개입(밑줄 파일 제외 등)을 통째로 끈다.
- **docs/deploy-guide.html** — 요청받은 HTML판 가이드. 외부 리소스 0(오프라인에서도, Pages에 올려도 열린다), 라이트/다크 자동 대응, 인쇄 대응. 두 Pages 설정 방법을 표로 비교했고 체크리스트를 넣었다. 헤드리스 Chrome 렌더링으로 확인.

---

## 제품 완성도 라운드 계획 (2026-07-23)

설계(specs/2026-07-23-product-completeness-design.md)를 Task 16개 계획(plans/2026-07-23-product-completeness.md)으로 옮기며 확정한 것들.

- **VAPID 공개키는 저장하지 않고 개인키에서 계산한다** — py_vapid로 유도하면 Render 환경변수가 설계대로 3개(FIREBASE_SERVICE_ACCOUNT·VAPID_PRIVATE_KEY·VAPID_SUBJECT)로 끝난다. 공개키를 4번째 변수로 두면 쌍이 어긋나는 사고(구독은 옛 공개키, 발송은 새 개인키)가 가능해진다.
- **푸시 스레드는 테스트에서 autouse로 차단** — POST 테스트 중 데몬 스레드가 별도 DB 커넥션으로 테스트 DB를 건드리는 사고를 막기 위해 `_no_push_threads` autouse 픽스처가 `send_to_guardian_async`를 무력화한다. 푸시 로직 자체는 동기 함수 `send_to_guardian`을 직접 호출해 검증한다.
- **vapid-key 엔드포인트는 기기 API(Task 4)가 아니라 푸시 모듈(Task 5)에** — 공개키 유도 코드가 push.py에 있으므로 의존 순서상 그쪽이 맞다.
- **queue.js는 storage 주입 순수 모듈** — localStorage를 직접 잡지 않고 인자로 받아 Vitest에서 가짜 storage로 테스트한다. flush는 건당 1회 시도(`postFall(payload, 1)`) — 실패분은 다음 트리거(재접속·online·60초 주기·전송 성공)가 다시 시도하므로 큐 안에서 재시도를 중첩하지 않는다.
- **PWA 아이콘은 표준 라이브러리 파이썬으로 생성** — PIL 의존성 없이 struct+zlib로 단색 PNG 192/512를 만든다. 아이콘은 홈 화면 식별용이지 브랜딩이 아니다.
- **Flutter HTTP 래퍼는 기존 관례대로 무테스트** — http 모킹 인프라가 없고 listFalls·acknowledge도 테스트가 없다. 순수 로직(알림 소스 규칙)만 TDD.
- **알림 소스는 플랫폼당 정확히 하나** — Android=FCM(백그라운드는 OS가 notification부 표시, 포그라운드는 onMessage 로컬 알림, id=이벤트 id), iOS=폴링. 판별을 `notifiableFromPolling` 순수 함수로 빼 테스트 2개를 붙였다(Task 15).
- **FCM E2E는 occurred_at을 매번 바꿔야 한다** — 멱등 제약이 같은 (방, 시각)을 200으로 흡수하고 푸시를 안 보내므로, 같은 curl 재실행은 "알림이 안 온다"는 가짜 실패로 보인다. 계획 Task 14 Step 12에 명시.
- **이 머신에 개발 환경이 없다** — backend/.venv도 web/node_modules도 없는 상태(.gitignore 대상)라 계획에 "환경 준비" 절을 넣고 모든 pytest 명령을 `.venv/bin/python -m pytest`로 통일했다. npm install은 Task 7에 포함.
- **google-services.json은 커밋, 서비스 계정 키는 금지** — 전자는 앱 식별자(비밀 아님), 후자는 발송 권한 그 자체다. 계획 Task 14 Step 1과 DEPLOYMENT 6-1절에 구분을 명시했다.

## E2E 검증 라운드 (2026-07-24)

남은 수동 검증 중 이 머신에서 가능한 3건을 실행했다 — ① 감지 페이지 오프라인→온라인 큐 재전송 브라우저 E2E ② VAPID 키 생성 + 데스크톱 웹 푸시 E2E ③ iOS 시뮬레이터 첫 기동. 시작 전 3종 자동 테스트 기준선은 전부 통과(pytest 33 · vitest 20 · flutter 7). E2E 스크립트는 `scripts/e2e/`에 커밋했다(실행법은 그 README).

### iOS — Firebase SwiftPM이 배포 타깃 15.0을 요구한다

`flutter build ios --simulator` 첫 시도가 실패했다. `firebase-core`/`firebase-messaging` SwiftPM 패키지(CocoaPods 아님 — Podfile 없음)가 최소 iOS 15.0을 요구하는데 Runner가 13.0이었다. 에러 메시지 안내대로 `project.pbxproj`의 `IPHONEOS_DEPLOYMENT_TARGET` 3곳(Debug/Release/Profile)을 15.0으로 올리니 빌드 성공(37.7초). 시뮬레이터·실기기 모두 iOS 26.5라 15.0 인상의 실사용 영향은 없다.

- iPhone 17 Pro 시뮬레이터에 설치·실행 — 첫 실행이라 알림 권한 다이얼로그('Fall Guardian'에서 알림을 보내고자 합니다)가 떴고, 이는 `Notifications.init()`의 정상 동작이다. 프로세스 7분+ 생존, DiagnosticReports에 크래시 리포트 없음. iOS 경로는 `Platform.isAndroid` 가드 덕에 GoogleService-Info.plist 없이도 Firebase 초기화를 건너뛴다 — 설계대로다.
- `xcrun simctl privacy`는 `notifications` 서비스를 지원하지 않아(이 Xcode 기준) 다이얼로그를 프로그램으로 걷어낼 수 없었다. 화면 검증이 더 필요하면 Simulator GUI에서 허용을 누르면 된다.

### 큐 재전송 E2E — 8개 체크 통과

실제 Chrome(playwright-core, `channel: "chrome"`)으로 detect.html을 띄우고, API 오리진만 차단(`route.abort`)한 채 토큰+큐 2건을 심어 로드 → 차단 해제 후 `setOffline(true→false)`로 진짜 `online` 이벤트를 발화시켰다.

- 차단 중 POST 시도는 정확히 1건이었다 — flush가 head 실패 시 그 자리에서 중단하는 설계 그대로. online 후 2건 순서대로 재전송, 큐 비움, 서버 DB의 방·발생시각 일치, 보낸 건 재적재 시 200 흡수·행 수 불변까지 확인.
- 실제 낙상→적재(enqueue) 경로는 카메라가 필요해 큐 선적재로 대체했다. 낙상→POST 자체는 3차 실측에서, 실패→적재 배선은 main.js의 catch 한 줄이라 위험이 낮다.

### 웹 푸시 E2E — 함정 3개를 넘어 9개 체크 통과

VAPID 키는 DEPLOYMENT 6-2의 명령 그대로 생성해 환경변수로 주입했다(문서 절차 검증 겸). 구독 공개키가 서버 파생(`/api/push/vapid-key/`)과 일치해 FCM 엔드포인트 구독·발송·수신이 한 번에 이어졌고, **헤드리스에서도 실수신까지 된다** — 재검증을 CI처럼 돌릴 수 있다는 뜻이다. 도달한 함정 3개는 scripts/e2e/README에도 요약했다.

- **Playwright 기본 컨텍스트는 시크릿 취급** — Chrome이 Push API를 의도적으로 막는다(기능 감지도 불가). `launchPersistentContext`로 일반 프로필을 써야 한다.
- **기본 인자 `--disable-background-networking`이 GCM 소켓을 끊는다** — 구독·기기 등록·발송(FCM 2xx)까지 전부 성공하는데 수신만 조용히 안 되는, 원인 찾기 고약한 증상이었다. 서버 로그에 발송 실패가 없다는 것으로 서버 쪽을 소거한 뒤 이 플래그를 지목했다. `ignoreDefaultArgs`로 제거하면 헤드리스에서도 수신된다.
- **알림 감지→수집을 두 evaluate로 나누면 레이스** — `waitForFunction`이 알림을 보고도 직후 스냅숏에서 사라져 있었다. 한 evaluate 안에서 100ms 간격 샘플링으로 감지와 캡처를 원자화해 해결.
- 덤으로 확인한 것 — pywebpush 기본 `ttl=0`(즉시 전달 아니면 폐기)이라 보호자 브라우저가 순간이라도 끊겨 있으면 그 푸시는 유실된다. 보호자 페이지 5초 폴링과 낙상 목록이 백업이라 제품 동작상 치명적이지 않아 `push.py`는 건드리지 않았다. 나중에 유실이 문제가 되면 `webpush(..., ttl=60)` 한 줄이 후보다.

### 남은 수동 검증

Android 실기기/에뮬레이터 백그라운드 FCM 수신(Firebase 콘솔 발급 필요), iPhone 홈 화면 PWA 푸시(HTTPS 배포 필요) — 둘 다 이 머신 밖 준비물이 필요하다.

## 배포 제거 (2026-07-24)

"배포 준비 (2026-07-18)"의 결정을 되돌렸다. Render·GitHub Pages에 배포하지 않기로 하고, **같은 와이파이(LAN) 안에서만 실행하는 전제**로 코드·문서를 정리했다.

- **삭제한 것** — render.yaml, .github/workflows/deploy-pages.yml, backend/build.sh, .nojekyll, 루트 index.html·pre.html(Pages 브랜치 배포 안전망), docs/DEPLOYMENT.md, docs/deploy-guide.html.
- **웹 API 주소는 접속 호스트를 따라간다** — web/js/api.js가 `http://${location.hostname}:8000`. 정적 서버와 Django가 같은 Mac에서 뜨므로 localhost로 열든 Mac IP로 열든 항상 맞는 주소가 된다. PROD 분기(onrender.com)는 삭제.
- **앱은 실기기에 API_HOST가 필수가 됐다** — kReleaseMode 분기(릴리즈=배포 주소)를 제거해 우선순위가 API_HOST > 시뮬레이터 기본값뿐이다. 실기기는 디버그·릴리즈 모두 `--dart-define=API_HOST=<Mac IP>`.
- **CORS는 전부 허용** — 같은 와이파이 기기가 `http://<Mac IP>:5500` 오리진으로 접속해 오리진을 특정할 수 없으므로 `CORS_ALLOW_ALL_ORIGINS = True`. 이로써 "포트 5500 강제"도 풀렸다(e2e 스크립트는 여전히 5500을 하드코딩하니 관례로 유지).
- **Django는 로컬 고정** — RENDER 분기, Postgres(dj-database-url·psycopg), whitenoise, gunicorn을 걷어내고 DEBUG=True·SQLite 고정. LAN 접속을 받으려면 `runserver 0.0.0.0:8000`으로 띄운다(README 갱신 — 기본 127.0.0.1 바인딩이면 폰이 못 붙는다).
- **웹 푸시는 localhost 한정이 됐다** — 서비스 워커·Push API는 보안 컨텍스트(https 또는 localhost)를 요구한다. https가 사라졌으므로 "알림 켜기"는 Mac 브라우저(127.0.0.1)에서만 되고, **아이폰 홈 화면 PWA 푸시는 범위 밖이 됐다**(checklist 항목 제거). 발송 자체는 로컬 서버로도 나간다 — 푸시 서비스는 브라우저 벤더의 인터넷 서버라서다. getUserMedia도 같은 제약이라 감지 페이지는 종전대로 Mac에서 연다.
- **VAPID·FCM 설정 절차는 README "푸시 알림 (선택)" 절로 이관** — DEPLOYMENT.md 6절이 유일한 사본이었다. docs/firebase-setup.html의 "Render 환경변수" 단계는 "환경변수를 붙여 runserver 실행"으로 고쳤다.

## Android 지원 제거 (2026-07-24)

"이 프로젝트는 Android에서 실행하지 않는다"는 결정에 따라 Android 관련 코드·설정·문서를 걷어냈다. 이 저장소에서 Firebase(FCM)는 오직 Android 백그라운드 푸시 한 가지를 위해 존재했으므로(제거 전 firebase-setup.html에 명시) FCM 스택 전체가 함께 나갔다.

- **삭제한 것** — app/android/ 전체, app/lib/push.dart(+push_test.dart), firebase_core·firebase_messaging 의존성, 백엔드의 `_send_fcm`·`_ensure_firebase`·firebase_admin 의존성과 `FIREBASE_SERVICE_ACCOUNT`, docs/firebase-setup.html.
- **"플랫폼당 알림 소스 1개" 규칙이 소멸했다** — Android=FCM / iOS=폴링 분리를 위해 있던 `notifiableFromPolling`(과 그 테스트 2개, push_test 1개)을 제거하고 폴링이 항상 알린다. 앱 테스트 7→4개, 백엔드 33→32개.
- **PushDevice.kind는 webpush 단일 choice로 남겼다** — 필드를 없애면 웹 쪽 API 계약(guardian.js가 kind를 보낸다)까지 바뀌므로 choices만 좁혔다(마이그레이션 0003). `kind="fcm"`은 이제 400이다(테스트로 고정).
- **iOS 배포 타깃 15.0은 유지** — Firebase SwiftPM 때문에 올렸지만 되돌릴 실익이 없고 이미 15.0으로 빌드 검증됐다. Firebase가 빠지면서 원격 SwiftPM 의존성이 0이 되어 Package.resolved 두 파일은 빌드가 삭제했다(정상). 빌드도 수 분 → 12초로 줄었다.
- **web 쪽 코드는 변경 없음** — 웹 푸시는 표준 VAPID(pywebpush)라 Firebase와 무관하다.
- **pubspec.lock의 shared_preferences_android 등은 남는다** — 유지하는 플러그인들의 연합(federated) 플랫폼 패키지라 pub이 관리하는 잔존이고, iOS 빌드에 포함되지 않는다.
- **과거 설계·플랜 문서(docs/superpowers/)는 이력으로 보존** — 본문을 다시 쓰는 대신 문서 머리에 제거 공지를 달았다.

## 백엔드 Spring Boot 교체 (2026-07-25)

- **Boot 버전은 4.1.0** — start.spring.io 스캐폴딩 시점 최신 안정판(고정 지정 안 함, 스펙대로). Gradle 9.5.1, Java 21 toolchain.
- **Jackson은 3세대** — `./gradlew dependencies --configuration runtimeClasspath | grep -i jackson` 확인 결과 `tools.jackson.core:jackson-databind:3.1.4`(`tools.jackson:jackson-bom:3.1.4`)가 실제 런타임에 올라간다. 단 `jackson-annotations`는 여전히 `com.fasterxml.jackson.core:jackson-annotations:2.21` — 브리프가 예상한 대로 어노테이션 패키지는 유지된다. Task 6(PushService·MartijnDwarsWebPushClient)에서 `ObjectMapper` 직접 주입 시 `tools.jackson.databind.ObjectMapper` 계열 import를 써야 한다(Jackson 3 세대).
- **Boot 4.x 스타터 명칭이 브리프 예시와 다르다** — `spring-boot-starter-web` 대신 `spring-boot-starter-webmvc`, `flyway-core` 대신 `spring-boot-starter-flyway`(+`flyway-database-postgresql`는 동일). 테스트 쪽은 `spring-boot-starter-test` 하나가 아니라 기능별로 쪼개져 `spring-boot-starter-data-jpa-test`·`spring-boot-starter-flyway-test`·`spring-boot-starter-security-test`·`spring-boot-starter-validation-test`·`spring-boot-starter-webmvc-test`가 각자 딸려온다(내부적으로 전부 `spring-boot-starter-test`를 물고 들어옴). 스캐폴딩 산출물을 정본으로 삼았다.
- **`AutoConfigureMockMvc` 패키지가 이동했다** — Boot 4.1.0에서 `org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc`(브리프 예시)는 더 이상 존재하지 않고 `org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc`(`spring-boot-webmvc-test` 모듈)로 옮겨졌다 — Boot 4.x가 테스트 자동설정을 기능별 모듈로 쪼갠 결과. `HealthApiTest.java`는 새 경로로 import를 고쳐 작성했다. `MockMvc`·`MockMvcRequestBuilders`·`MockMvcResultMatchers`(org.springframework.test.web.servlet.*)와 `SpringBootTest`는 브리프 그대로 유효 — spring-test·spring-boot-test 모듈은 안 쪼개졌다. 이후 태스크에서 `@AutoConfigureMockMvc`를 쓸 때는 이 새 import를 따라야 한다.
- **Security 자동설정 경고는 무해하다** — bootRun 시 "Using generated security password" 경고가 뜨는데, UserDetailsService 빈이 아직 없어서(Task 3에서 실 인증 도입 전) Boot가 기본 인메모리 사용자를 자동 생성하기 때문이다. `SecurityConfig`가 `httpBasic`·`formLogin`을 껐고 `permitAll("/")` 외엔 인증을 요구하므로 실질적 영향은 없다 — Task 3에서 실제 인증이 들어가면 사라질 것으로 예상.
- **(Task 6) MartijnDwarsWebPushClient의 ObjectMapper import는 `tools.jackson.databind.ObjectMapper`/`JsonNode`로 확정** — 위 Jackson 3 확인의 실제 적용. `PushService`의 `@JsonProperty`만 여전히 `com.fasterxml.jackson.annotation.JsonProperty`(어노테이션 계열은 2.21 그대로).
- **(Task 6) `nl.martijndwars:web-push:5.1.2`의 실제 시그니처는 브리프 스니펫과 완전히 일치** — `PushService(String publicKey, String privateKey, String subject)`, `Notification(Subscription, String payload)`, `send(Notification)` 반환 `org.apache.http.HttpResponse`(`getStatusLine().getStatusCode()`) 전부 javap로 확인. 브리프의 "다르면 파일 안에서만 고치라"는 우려는 API 형태 자체엔 해당 없었다.
- **(Task 6) 다만 컴파일 클래스패스 이슈가 있었다** — web-push의 POM이 `httpclient`(`org.apache.http.HttpResponse`)와 `jose4j`(`JoseException`)를 Maven `runtime` scope로 선언해서, `./gradlew dependencies --configuration compileClasspath`에는 안 잡히고 `runtimeClasspath`에만 잡힌다. `MartijnDwarsWebPushClient`가 `send()`의 반환 타입·체크 예외를 컴파일 타임에 참조하므로 `compileJava`가 "class file not found"로 실패했다. `WebPushClient` 인터페이스는 손대지 않고, `build.gradle`에 `implementation 'org.apache.httpcomponents:httpclient:4.5.13'`과 `implementation 'org.bitbucket.b_c:jose4j:0.7.9'`를 명시 추가해 해결(런타임에 이미 해석되던 버전과 동일해 충돌 없음).
- **(Task 6) Jackson 3의 `JsonNode.asText()`는 deprecated** — `asString()`으로 교체됨(javap 확인). `MartijnDwarsWebPushClient`에서 `asText()` 대신 `asString()`을 사용해 컴파일 경고 없이 클린 빌드.
- **(Task 7) Instant 직렬화 컨틴전시는 발동하지 않았다** — 브리프가 우려한 `.000Z` 소수초·epoch 숫자 직렬화가 실제로는 나타나지 않았다. `FallEventResponse`의 `Instant` 필드를 별도 `@JsonFormat` 없이 그대로 뒀는데도 `duplicate_post_returns_200_and_no_new_row`·`post_accepts_offset_and_stores_utc`의 `jsonPath("$.occurred_at").value("2026-07-23T03:00:00Z")` 단언이 첫 실행부터 그대로 통과했다. Spring Boot의 Jackson 자동설정이 기본값으로 `WRITE_DATES_AS_TIMESTAMPS`를 꺼두기 때문으로 보이며(Jackson 3 세대에서도 이 기본값이 유지됨), `application.yml`에 별도 Jackson 설정을 추가하지 않았다.

### Task 8 통합 검증 (2026-07-25)

스펙 9절의 성공 기준 세 겹(서버 JUnit·클라이언트 회귀·E2E)을 실제로 돌렸다. 서버 36(`./gradlew test`) · 웹 20(`npm test`) · 앱 4(`flutter test`) 전부 통과, `git status --porcelain web/ app/` 빈 출력으로 클라이언트 무수정도 재확인했다. E2E 2종(`scripts/e2e/queue-e2e.mjs`·`push-e2e.mjs`)도 각 8·9개 체크 전부 통과했고 헤드리스만으로 됐다(`--headed` 재시도는 필요 없었음). Boot 4.1.0·Jackson 3(`tools.jackson.databind:3.1.4`) 구성은 Task 1·6이 확인한 그대로다.

- **발견 1 — 정적 서버는 `npx serve`가 아니라 `python3 -m http.server`여야 한다.** `npx serve -l 5500 .`(serve 14.2.6)의 기본 clean-URL 동작이 `.html` 요청을 301로 확장자 없는 경로로 리다이렉트하면서 쿼리스트링까지 통째로 버린다 — `curl -sI "http://127.0.0.1:5500/index.html?next=guardian.html"` → `Location: /index`(next 소실). `queue-e2e.mjs`의 `detect.html` URL 유지 검사, `push-e2e.mjs`의 로그인 후 `guardian.html` 리다이렉트 검사가 이 때문에 실패했다 — 리다이렉트가 정적 서버 계층(포트 5500)에서 백엔드 요청 전에 이미 일어나므로 Django든 Spring이든 무관하게 재현된다. `scripts/e2e/README.md`가 원래 지정한 `python3 -m http.server 5500 -d web`으로 바꾸자 두 실패 모두 사라졌다 — 코드 변경 없이 서버 기동 명령만 README 원본대로 쓰면 되는 문제였다.
- **발견 2(서버 fix) — `nl.martijndwars:web-push`의 인자 없는 `send(Notification)`이 레거시 인코딩으로 기본 동작해 FCM이 거부했다.** 웹 푸시 E2E의 실제 수신 단계가 헤드리스·헤디드 모두에서 실패했고, 서버 로그에 `403 permission denied: crypto-key header had invalid format`이 남았다(VAPID 키 파생·구독 자체는 정상 — `/api/push/vapid-key/` 응답이 `npx web-push generate-vapid-keys`의 Public Key와 정확히 일치함을 확인). javap로 뜯어보니 `send(Notification)`은 구식 `Encoding.AESGCM`(별도 `Crypto-Key` 헤더 조합)으로 기본 설정되는데, FCM이 이 조합을 더 이상 받아주지 않는다. `MartijnDwarsWebPushClient.send()`에서 `Encoding.AES128GCM`(RFC 8292 표준 — VAPID를 `Authorization: vapid t=...,k=...` 헤더 하나로 통합)을 명시 지정해 해결(fix 커밋 `7bb96d3`). `WebPushClient` 인터페이스는 그대로라 서버 테스트 36개는 재실행해도 36/0 그대로였고, 이 수정 후 재실행한 push-e2e는 헤드리스에서도 9개 체크 전부 통과(알림 제목 "낙상 감지", 본문 "부엌 3에서 낙상 감지"까지 확인).

## ALERTED 재무장 버그 (2026-07-25)

전체 코드 점검에서 찾은 확인된 버그 1건이다. `detector.js`의 `NO_PERSON` 복귀 분기가 `stateBeforeGap`으로 `FALLEN`·`FALLING`만 다루고 `ALERTED`를 빠뜨렸다. 그래서 알림이 나간 뒤 넘어진 사람이 가구에 2초 이상 가려졌다가 **여전히 바닥에 누운 채** 재검출되면 상태가 `STANDING`으로 떨어졌다. 바로 아래 `case "ALERTED"`의 주석이 단정한 불변식("다시 일어나야만 재무장된다")을 코드가 스스로 깨고 있었다.

- **증상은 미탐지가 아니라 중복 알림이다** — 재무장된 뒤에는 이미 `tilt`가 60°를 넘어 있으므로, 누운 채 몸을 크게 뒤척여 `hipVelocity`가 임계값을 넘으면 2차 관문이 **한 프레임에** 열려 `FALLEN`이 되고 5초 후 두 번째 전송이 나간다. 재현 테스트에서 fall 2건을 확인했다.
- **서버 멱등성으로는 막을 수 없다** — `uniq_fall_dedup` 키가 `(guardian, room_name, room_number, occurred_at)`이고 `occurred_at`은 `fallingAt`이다. 두 번째 `fallingAt`은 다른 값이라 새 행 + 새 푸시가 된다. 오프라인 큐 재전송 중복과 달리 이건 DB 계층에서 흡수할 방법이 없어서 판정 쪽을 고쳐야 했다.
- **왜 이 경로만 비어 있었나** — `FALLEN`·`FALLING` 복귀에는 테스트가 3개(끊김 후 바닥 재검출, FALLING 중 끊김, 끊긴 동안 자가 회복) 있었는데 `ALERTED` 중 끊김만 없었다. 세 관문을 통과한 *뒤*의 상태 전이가 사각지대였다.
- **수정은 첫 분기 추가 하나** — `stateBeforeGap === "ALERTED" && tilt > TILT_FALLEN`이면 `ALERTED`로 되돌린다. `fallenAt`은 건드리지 않는다(ALERTED가 쓰지 않는 값이다). 기존 `FALLEN`/`FALLING` 분기는 그대로 뒀다. 나중에 실제로 일어나면 `case "ALERTED"`의 `tilt < TILT_UPRIGHT` 조건이 정상적으로 재무장한다.
- **같이 점검했으나 버그가 아니었던 것** — "`NO_PERSON_TIMEOUT`(2초) 미만의 짧은 미검출은 Δt를 줄여 가짜 속도를 증폭시킨다"는 가설도 세워 테스트로 확인했는데 재현되지 않았다. `EMA_ALPHA` 0.4가 단일 프레임 스파이크를 눌러서, 임계값을 넘기려면 raw 속도가 1.125 이상이어야 한다. 짧은 끊김은 `resetMotion()`을 타지 않지만 EMA가 실질적 방어가 되고 있다.

## 백엔드 구현 설명 문서 (2026-07-26)

Spring Boot 백엔드를 공부용으로 설명하는 `docs/backend-architecture.html`을 만들었다. Django 시절 같은 경로에 같은 이름의 문서가 있었는데 백엔드 교체(13절) 커밋 `c32324a`에서 삭제됐고, 교체 설계 문서(스펙 1절)가 "Spring 버전 아키텍처 문서"를 명시적으로 범위 밖으로 미뤄뒀다. 이번 라운드가 그 후속이다.

- **대상 독자를 "Spring 기본은 아는 백엔드 입문자"로 좁혔다** — 처음엔 어노테이션·DI·JPA부터 설명하는 안도 검토했지만 사용자가 후자를 골랐다. 이 결정이 분량을 절반 이하로 줄였고 초점을 "무엇을 했는가"에서 **"왜 그렇게 했는가"**로 옮겼다. 문서가 설명하는 것도 대부분 관행을 벗어난 자리들이다 — `FallService`에 클래스 `@Transactional`이 없는 것, `save` 대신 `saveAndFlush`인 것, 403이 아니라 404인 것, 푸시 예외를 삼키는 것.
- **구조는 "요청 한 건의 여정 → 주제별 심화"** — 계층별 순회(config→domain→…→controller)와 엔드포인트별 나열도 후보였다. 계층별은 멱등성·소유권처럼 **여러 계층에 걸친 주제가 흩어지고**, 엔드포인트별은 사실상 API 명세서가 되어 설계 의도가 안 드러난다. `POST /api/falls/` 한 건을 필터부터 비동기 푸시까지 끝까지 따라간 뒤 주제를 파고드는 방식이 이 백엔드에 맞았다 — 이 요청 하나에 인증·검증·소유권·멱등성·푸시가 전부 등장한다.
- **삭제된 Django 문서에서 CSS 골격만 재사용하고 내용은 전면 재작성했다** — 색 변수·다크모드·박스/표/코드블록 스타일은 그대로 쓰고, 계층 다이어그램(`.layers`)과 번호 매긴 여정 스텝(`.flow`)은 새로 만들었다. 외부 리소스(CDN·폰트·이미지) 의존은 0으로 유지했다.
- **재사용한 CSS에 렌더링 버그가 하나 있었다** — `.box b:first-child { display: block }`이 박스 **제목**만 겨냥한 규칙인데, CSS `:first-child`가 텍스트 노드를 세지 않기 때문에 박스 안 문단 중간의 `<b>` 강조까지 잡아 블록으로 만들었다. "…그 트랜잭션은 **중단(aborted) 상태**가 되고"에서 강조 부분만 별도 줄로 튀어나갔다. 7곳이 영향받고 있었고 `.box > b:first-child`로 직계 자식에 한정해 해결했다. **정적 검사로는 안 잡히는 종류라** macOS `qlmanage`로 실제 렌더를 봐서 발견했다 — HTML 문서를 만들 때 태그 균형·링크 검증만으로는 부족하다는 사례다.
- **검증은 세 겹으로 했다** — ① 문서가 인용한 `파일:줄번호` 26개를 `sed`로 실제 소스와 한 줄씩 대조(전부 일치), ② 코드블록의 모든 코드 줄이 소스에 실존하는지 스크립트로 대조(불일치 20건은 전부 curl·JSON 예시, 줄끝 주석 이동, 정렬 공백 차이로 확인), ③ `./gradlew test --rerun-tasks`로 테스트 수를 직접 세어 36개 확인. README의 "36개"가 맞았고, `grep -c "@Test"`로 세면 39가 나오는데 이는 `@TestConfiguration`이 접두사로 걸리기 때문이다 — 테스트 개수는 grep이 아니라 `build/test-results/test/*.xml`에서 세야 한다.
- **문서에서 소스와 의도적으로 다르게 쓴 곳** — `FallEvent.acknowledgeNow()`의 `java.time.temporal.ChronoUnit.MICROS`를 `ChronoUnit.MICROS`로 줄였고, `FallEventRequest`는 검증 어노테이션을 생략한 축약형으로 실었다(생략했다는 사실을 코드블록 주석으로 명시). 그 외 코드 발췌는 정렬 공백을 빼면 원문 그대로다.
- **긴 코드 줄은 4개만 남겼다** — 840px 컨테이너에서 한 줄에 들어가는 폭이 약 94칸인데, 처음엔 9줄이 넘쳤다. 그중 내가 임의로 정렬해서 넓어진 블록 2개(`AuthService.signup`·`RoomService.validate`)는 **원문 형태로 되돌리자 폭 문제도 같이 사라졌다** — 읽기 좋게 만들려던 정렬이 오히려 문제였던 셈이다. 남은 4줄은 진짜 원문이 긴 경우라 `overflow-x: auto`로 스크롤시킨다.

## 발표자료 타겟 재정의 (2026-07-26)

`web/pre.html`을 13장에서 19장으로 늘렸다. 요청은 네 가지였다 — 타겟을 독거노인에서 보호자로 이동, 부모부양세대 통계, 제주 검증 후 전국 확대, 비즈니스 모델 선정. 분량(19장)·시각자료 비중(캡쳐+자체 차트 병행)·BM 조합(B2C+B2G+B2B 3트랙)은 착수 전에 확인받고 정했다.

- **타겟 전환의 논거를 "두 개의 벽"으로 세웠다** — 사용자가 준 전제는 "독거노인은 인터넷이 안 되는 분이 많다"였는데, 조사해 보니 이것만으로는 약했다. 고령층 인터넷 이용률은 76.9%(통계청 2025 고령자 통계)로 *대부분은 쓴다*. 그래서 인프라 하나에 기대지 않고 **지불 능력**을 두 번째 축으로 세웠다 — 노인 소득 빈곤율 39.7%로 OECD 1위(평균 14.8%의 2.7배). 결정적인 건 두 축이 겹치는 지점이다. **낙상 위험이 가장 높은 75세 이상이 빈곤율도 가장 높다(54.0%).** 우리가 가장 지키고 싶은 사람이 가장 돈을 못 내는 사람이라는 구조가 드러나면서, "사용자와 결제자를 분리한다"는 결론이 자연스럽게 따라 나왔다.
- **최초 페르소나를 지우지 않고 5장에 남겼다** — 사용자가 "오랜 고민을 했다는 흔적"을 원했다. 다만 단순 회고로 두면 자리만 차지하므로 *문제 정의는 유효하고 바뀐 건 판매 대상뿐*이라는 구조로 썼다. 폐기가 아니라 분리라는 점이 6장의 "어르신에게는 안전을, 보호자에게는 안심을 판다"로 이어지고, 16장 B2G 트랙이 "인터넷도 돈도 없는 어르신을 공공 예산으로 커버"하며 최초 타겟을 회수한다. 세 슬라이드가 하나의 논리로 묶인다.
- **"제주가 전국의 미래"라는 흔한 프레이밍은 사실이 아니어서 버렸다** — 초안에서 쓰려던 "제주가 먼저 늙으니 전국의 축소판"은 데이터가 반대였다. 전국이 이미 20.3%로 초고령사회에 진입했고 제주는 19.0%로 **오히려 뒤에 있다.** 그대로 발표하면 심사에서 깨질 자리였다. 대신 실제로 제주가 1위인 지표로 갈아탔다 — **고독사 연평균 증가율 43.6%, 전국 평균 5.6%의 7.7배로 전국 1위.** 이건 우연히도 우리가 푸는 문제(발견 지연) 그 자체라, "가장 먼저 늙는 곳"보다 "가장 먼저 위험해지는 곳"이 논리적으로도 더 강했다. 실증 조건(인구 66만·섬이라 경계가 닫힘·기초자치단체 없는 단일 광역행정)을 근거로 붙이고, 전국이 이미 초고령사회라 **검증 즉시 시장이 열려 있다**는 점으로 마무리했다.
- **BM은 "같은 코드로 세 시장"이라는 한 문장으로 묶었다** — 트랙을 나열만 하면 산만해진다. 하드웨어 원가가 0이라는 기존 기술적 사실이 곧 세 시장 동시 대응의 근거가 되도록 구성했고, 17장에서 이를 **방 개수별 첫 해 총비용** 차트로 시각화했다. 우리만 평평한 직선이고 렌탈 CCTV·레이더는 우상향한다. 경쟁 우위를 말로 주장하지 않고 기울기로 보여주는 편이 강해서, 단일 가격 비교 막대 대신 이 형태를 골랐다.
- **팔레트는 눈으로 고르지 않고 검증기를 돌렸다** — dataviz 스킬의 `validate_palette.js` 기준으로 기존 덱의 blue(#3987e5) / neutral(#6b6a66) 쌍은 통과했다(CVD ΔE 19.1, 정상시야 19.4, 대비 모두 PASS). gray가 chroma floor에서 FAIL로 뜨지만 이건 *의도된 중립색*이라 해당 없음이다. 반면 깔때기용으로 만들려던 **4단계 파랑 램프는 인접 단계 ΔE 6.3으로 하드 FAIL** — 정상 시야로도 구분이 안 되는 수준이었다. 3단계 명도 단조 램프로 줄이고 각 밴드에 직접 라벨을 붙여 색이 크기를 *보조만* 하도록 바꿨다. 17장 3계열 선그래프도 색을 하나 더 늘리는 대신 실선/점선으로 갈랐다.
- **정적 검사로는 못 잡는 레이아웃 버그를 렌더로 잡았다** — 백엔드 문서 라운드에서 얻은 교훈이 그대로 재현됐다. 19장을 헤드리스 Chrome으로 전부 렌더해 보고서야 ① 7장 깔때기의 SOM 밴드에서 라벨과 값이 겹치는 것(밴드 폭을 좁게 잡은 탓), ② 높이가 늘어난 카드 안에서 내용이 위로 쏠려 큰 여백이 생기는 것 두 가지를 발견했다.
- **다만 "여백은 무조건 없앤다"가 답은 아니었다** — 16장 BM 3트랙 카드에 `justify-content:center`를 넣었더니 카드마다 내용 높이가 달라 **배지·과금 타일이 서로 어긋났다.** 비교용 3열 레이아웃에서는 세로 중앙 정렬보다 **열 간 정렬**이 중요해서 top 정렬로 되돌렸다. 5·6장처럼 카드가 독립적으로 읽히는 자리에서만 중앙 정렬이 맞다.
- **테스트 개수를 `grep -c "@Test"`로 세다가 이 문서가 이미 경고한 함정에 그대로 빠졌다** — 39개가 나와서 슬라이드에 그렇게 적었는데, 바로 위 "백엔드 구현 설명 문서" 절이 **`@TestConfiguration`이 접두사로 걸려 39가 나오니 `build/test-results/test/*.xml`에서 세라**고 적어둔 그 함정이었다. XML 기준 실제 36개(`--rerun-tasks`로 재실행해 36/0 확인). 슬라이드를 36·합계 62로 고쳤다. 과거 노트를 먼저 읽었으면 피할 수 있었던 실수다.
- **발표자료가 코드보다 뒤처져 있던 것도 같이 고쳤다** — 1장 배지와 13장 아키텍처가 아직 `Django + SQLite`·`5초 폴링`이었다. 실제로는 Spring Boot 4.1 + PostgreSQL이고 웹 푸시(RFC 8291/8292)가 들어가 있다. 백엔드 교체(13절) 때 발표자료가 갱신 대상에서 빠져 있었다.

## Android 재지원 (2026-07-27)

2026-07-24에 걷어낸 Android를 다시 붙였다. 앞 절을 지우지 않고 남겨 둔다 — 없앴다 다시 붙인 경위 자체가 기록이다.

**되살린 것과 되살리지 않은 것이 다르다.** 제거 라운드는 `android/` 폴더와 FCM 스택을 함께 걷어냈다. 이번엔 폴더와 로컬 알림만 돌아왔고 **FCM은 되살리지 않았다.** Firebase 의존성·서비스 계정·백엔드 발송 경로가 전부 따라 들어오는데, 이 프로젝트가 실제로 얻는 것은 "앱이 꺼져 있을 때의 알림" 하나다. 보호자 페이지의 표준 웹 푸시가 그 자리를 이미 메우고 있어서 값이 비용을 못 넘는다. 그래서 앱은 여전히 켜 둬야 알림이 온다 — iOS와 같은 조건이다.

**작업의 실체는 만드는 것이 아니라 걷어내는 것이었다.** `lib/`의 Dart 파일 10개 중 9개가 이미 플랫폼 중립이었다. 화면 5개·`models.dart`·`poller.dart`·`main.dart`는 한 줄도 안 고쳤다. 실제로 갈라진 곳은 알림과 서버 주소 둘뿐이다. 화면을 고쳐야 하는 상황이 왔다면 그건 설계가 틀렸다는 신호였다.

**알림 설정을 테스트가 붙잡게 했다.** `InitializationSettings`에 `android` 필드가 없으면 안드로이드에서 예외도 로그도 없이 알림이 그냥 안 뜬다. 낙상 앱에서 알림이 안 뜨면 앱이 하는 일이 없는데, `flutter analyze`는 이걸 못 잡고 이 맥엔 Android SDK가 없어 실기기 검증도 못 한다. 순수 단위 테스트가 유일하게 가능한 검증이라 `_details`의 밑줄을 떼고 `settings`를 상수로 꺼냈다(`@visibleForTesting`). 테스트 때문에 공개 범위를 넓힌 것이고, 조용히 실패하는 버그를 잡는 값이 그 비용보다 컸다.

**Flutter Web은 기술이 아니라 중복 때문에 안 했다.** 검토 중 `flutter_local_notifications` v22가 웹을 지원한다는 것을 확인했다(`flutter_local_notifications_web` 1.0.0, `InitializationSettings.web` 필드 존재). 장벽은 없었다. 다만 `guardian.html`이 낙상 목록·방 관리·연락처를 이미 하고 **웹 푸시까지** 된다. Flutter Web을 붙이면 보호자 웹 클라이언트가 2개가 되고 새 쪽이 오히려 기능이 적다.

**`10.0.2.2` 분기의 대가는 웹 빌드다.** `Platform.isAndroid`를 쓰느라 `api.dart`에 `dart:io`가 들어왔고, 이 파일은 이제 웹으로 컴파일되지 않는다. Android 전용으로 정한 지금은 맞는 선택이지만, 나중에 웹을 붙이면 조건부 임포트(`dart:io`/`dart:html`)로 갈라야 한다. 위 문단에서 웹을 안 하기로 한 것과 한 세트인 결정이다.

**평문 HTTP는 도메인을 좁히지 않고 전역으로 열었다.** 정밀한 방법은 `network_security_config.xml`에 특정 도메인만 쓰는 것인데, 접속 대상이 그때그때 달라지는 LAN IP라 화이트리스트를 미리 쓸 수가 없다. 매번 바뀌는 값을 넣어 둔 설정 파일은 정밀해 보일 뿐 정밀하지 않다. `usesCleartextTraffic="true"` 한 줄에 이유를 문서로 남기는 쪽이 정직하다. 외부 배포 없는 LAN 전용 과제 제출물이라는 전제에서만 성립하는 선택이다.

**`flutter create`가 조용히 망가뜨리는 것이 셋 있었다.** 실행 후 `git status`로 잡아냈다. ① `.metadata`의 `migration.platforms`에서 `ios` 항목이 `android`로 **대체**된다(추가가 아니다) — 손으로 되살렸다. ② 같이 도는 `pub get`이 `flutter_local_notifications`를 22.0.1 → 22.2.0으로 올린다 — 설계의 API 확인을 22.0.1 소스로 했으므로 lock을 되돌렸다. ③ 카운터 앱 예제 `test/widget_test.dart`를 만드는데, 없는 `MyApp`을 참조해 `flutter test` 전체를 컴파일 에러로 죽인다 — 삭제했다. 셋 다 "새 폴더만 생겼겠지"라고 넘어가면 나중에 이상한 자리에서 터진다.

**미검증인 채로 끝냈고 그걸 문서에 박아 뒀다.** 이 맥에 Android SDK가 없어 APK 빌드·에뮬레이터 접속·알림 표시·권한 팝업·`tel` 발신이 전부 미검증이다. 사용자가 이 상태를 알고 선택했다. "될 것이다"로 넘기지 않고 README 실행 절과 `checklist.md` 17절(체크 안 된 항목), `docs/manual-verification.md` 7절 세 곳에 남겼다. SDK를 설치한 사람이 7절부터 밟으면 된다.

**생성물을 안 건드린다는 원칙을 한 곳에서 깼다.** 계획은 `android/`를 `flutter create` 생성물로 두고 매니페스트만 손대기로 했는데, 최종 리뷰에서 `app/build.gradle.kts`에 core library desugaring이 빠진 것이 나왔다. `flutter_local_notifications` 22는 예약 알림을 안 써도 이 설정을 요구하고(플러그인 README), 본체가 `java.time`을 쓰는데 이 앱의 `minSdk`는 24이며, Flutter의 Gradle 툴링은 이걸 대신 넣어 주지 않는다. 없으면 첫 APK 빌드가 `checkDebugAarMetadata`에서 그냥 선다. 미검증 항목이 아니라 확정된 설정 누락이라 SDK 없이도 지금 고칠 수 있었고, 안 고치면 문서 네 곳의 "SDK만 설치하면 된다"가 거짓이 된다. **교훈은 이것이다** — "생성물은 손대지 않는다"는 깔끔한 제약이 의존성이 요구하는 앱 측 Gradle 설정이라는 사각지대를 만들었다. 플러그인을 붙이는 라운드에서는 API 시그니처만 볼 것이 아니라 그 플러그인 README의 Setup 절도 읽어야 한다.

**"그대로 둔다"는 테스트가 아무것도 안 지키고 있었다.** 같은 리뷰가 `notifications_test.dart`의 "iOS 설정은 그대로다"를 지적했다. `isNotNull`만 봐서 누가 `presentAlert`를 false로 뒤집어도 통과했다. 이 라운드의 가장 큰 리스크가 "Android를 붙이다 iOS를 흘리는 것"이고 그걸 막는 유일한 자동 가드였는데, 이름이 약속한 것을 지키지 않았다. Android 쪽과 같은 수준으로 여섯 값을 고정했고, 겸사겸사 고정 값 표에서 유일하게 안 덮이던 채널 이름 `낙상 알림`도 넣었다.
## 낙상 기록 삭제 — 완전 삭제와 판정 순서 (2026-07-27)

**소프트 삭제를 기각했다.** `deleted_at` 컬럼과 전 조회 경로의 필터를 더하는 대신 행을 지운다.
방 삭제(`RoomService.delete`)가 이미 완전 삭제라 한 프로젝트 안에서 삭제의 의미가 갈리면 안 되고,
"영상도 좌표도 남기지 않는다"는 이 프로젝트 기조와도 맞는다. Flyway 마이그레이션이 필요 없어진 것은
덤이다. 대가는 되돌릴 수 없다는 것이고, 그래서 웹은 `confirm()`, 앱은 `showDialog`로 확인을 받는다.

**미확인 삭제를 서버가 막는다.** "보호자가 확인하지 않은 낙상은 사라지지 않는다"는 안전 규칙이다.
UI에서 버튼만 숨기면 규칙이 아니라 장식이다. 비용은 `if` 한 줄과 테스트 하나였다.

**소유권 판정이 확인 여부보다 먼저다.** 순서를 뒤집으면 남의 미확인 기록에 400이 나가면서
"그 id는 존재하고 아직 미확인"이라는 사실이 새어나간다. `acknowledge`가 이미 쓰는
`findByIdAndGuardianId` 패턴을 그대로 따라 확인 여부와 무관하게 404를 준다.
테스트 `delete_other_users_event_is_404_regardless_of_acknowledgement`가 이 순서를 고정한다.

**앱 삭제는 목록 스와이프가 아니라 상세 화면에 뒀다.** 목록은 5초 폴링이 `_events`를 통째로
갈아치우는 자리다. `Dismissible`의 사라지는 애니메이션 도중 리스트가 교체되면 상태가 꼬인다.
상세 화면은 폴링 대상이 아니라 이 문제가 없고, 웹의 "확인 → 삭제" 순서와도 같아진다.

**삭제 신호는 새 타입 없이 `pop(null)`로 보냈다.** 상세 화면은 원래 `pop(_event)`로 갱신된 이벤트를
돌려주고 목록이 그 자리만 교체한다. 삭제를 알리려고 sealed 결과 타입을 만드는 대신, 목록의 null 경로에
`_refresh()`를 붙였다. 세 줄이고, 덤으로 "스와이프 백은 5초 폴링을 기다려야 한다"던 기존 아쉬움
(`fall_detail.dart`의 주석에 적혀 있던)도 같이 사라졌다.

**되살아나는 경우를 알면서 두었다.** 감지 페이지 오프라인 큐가 같은 낙상을 재전송하면 중복 조회가
실패해 새 행으로 살아난다. 다만 큐에 들어가려면 POST가 3회 실패해야 하고 그러면 서버에 행이 없어
삭제할 것도 없다. "행은 저장됐는데 응답만 유실된" 경우에만 성립한다. 막으려면 삭제한 조합의 묘비를
남겨야 하는데 완전 삭제를 택한 이유와 정면으로 어긋나서 대응하지 않았다.

## 음성 응답 확인·119 자동 신고 (2026-07-27)

**타임라인은 낙상 확정(FALLEN 진입)이 0초다.** 0s 마이크 ON → 5s 보호자 알림(기존 그대로) → 10s "괜찮으세요?" → 20s 무응답 신고. 보호자 알림 시점부터 다시 세는 안은 신고가 25초로 늦어져 기각했다. 20초는 TTS 재생과 무관한 절대 마감이라, ttsEnded가 영영 안 와도(TTS가 조용히 죽어도) 신고가 나간다.

**STT는 Web Speech API다.** 응급 구간(낙상 확정~해제·신고·회복)에만 마이크가 켜지는 조건으로, 인식 오디오가 구글 서버를 거치는 트레이드오프를 감수했다. 온디바이스(Vosk, 모델 ~50MB)는 과제 범위에 무겁고, 음량 기반 감지는 고통의 신음을 "괜찮음"으로 오인해 신고를 취소하는 치명적 결함이 있어 기각했다.

**자기 목소리 방어는 이중이다.** 질문 "괜찮으세요?"에 인식 키워드 "괜찮"이 들어 있다. 어댑터가 TTS 재생 동안 인식을 끄는 것이 1차, 상태머신이 ASKING 중 heardOk를 버리는 것이 2차다. 정지 직전 오디오의 인식 결과가 정지 후에 도착하는 경로가 있어 어댑터만으로는 못 막는다.

**신고는 새 엔드포인트 없이 재-POST다.** 같은 낙상 payload에 reported_119_at을 붙여 POST /api/falls/를 한 번 더 보낸다. 유니크 키 중복(200)이면 acknowledgeNow와 같은 멱등 병합 — 기존 값이 null일 때만 쓴다. 실패하면 기존 localStorage 큐가 그대로 실어 나른다(큐 코드 변경 0줄). 두 POST의 도착 순서가 뒤집혀도(신고분이 먼저 201, 원본이 나중에 200) 결과가 같고, 그 드문 경로의 푸시는 결함이 아니라 지연된 첫 알림이다.

**확정 전송 없이 신고에 이르는 드문 경로를 막지 않고 메웠다.** FALLEN 직후부터 계속 가려지면(NO_PERSON) detector의 5초 확정이 일어나지 못해 원본 payload가 없는 채 20초 마감이 온다. 에피소드를 끊는 대신(스펙 — NO_PERSON은 회복이 아니다), main.js가 FALLING 진입 시각을 따로 기억해 occurred_at을 원본과 같게 만들어 신고를 보낸다 — 시각이 같아야 나중에 원본이 도착해도 서버가 한 행으로 합친다. confidence는 확인할 길이 없어 0이다.

**실패는 전부 신고 쪽으로.** STT 오류·미지원·마이크 거부·TTS 실패는 heardOk가 영영 안 오는 것, 즉 무응답이라 20초에 신고된다. 확인 장치의 실패가 신고를 삼키면 안전망이 아니라는 판단이다. 대가는 오신고 가능성인데, 이 시스템의 신고는 실제 발신이 아니라 기록이라 감수할 만하다.

**상세 화면 재조회는 세대 카운터로 낡은 응답을 버린다.** 계획의 `_busy` 가드는 응답 도착 순간만 보므로, 확인 완료 전에 나간 listFalls 응답이 뒤늦게 도착하면 방금 갱신된 확인·신고 상태를 최대 5초간 되돌려 보일 수 있었다(태스크 리뷰 발견, 설계 수정 승인). `_actionEpoch`를 요청 시점에 스냅샷해 완료 시점과 다르면 응답을 버린다. 확인·삭제 완료가 +1한다.

## 창업 캠프 발표 피칭덱 (2026-07-28)

**pre.html 확장이 아니라 새 파일(web/pitch.html)로 만들었다.** pre.html은 "과제 프로젝트 발표"의 기술 서사로
완결돼 있고, 피칭덱은 시장·경쟁·BM이 축이라 한 파일에 섞으면 두 서사가 모두 흐려진다. 대신 디자인 토큰·
내비게이션 JS·기사 캡쳐 7장 중 5장을 그대로 계승해 시각 일관성을 지켰다. 대용량 base64 캡쳐 때문에 손으로
조립하지 않고 템플릿+파이썬 치환으로 생성했다. 산출물 자체가 단일 파일이라 pitch.html이 곧 원본이다.

**리서치가 사전과제 수치 두 곳을 교정했다.** ① "낙상의 72.5% 주택 발생"의 출처는 질병관리청이 아니라
통계청 통계개발원(KOSTAT 통계플러스, 2021년 자료)이다 — 질병관리청에는 의미가 전혀 다른 "75세 이상 손상
입원의 72.5%가 낙상"이 따로 있어, 출처를 섞으면 Q&A에서 혼동될 위험이 크다. ② "장시간 방치 시 사망 위험
약 2배"는 어떤 원문에서도 확인되지 않아 "1시간 이상 방치된 낙상자의 절반이 6개월 내 사망(Wild et al.,
BMJ 1981)"으로 교체했다. 사전과제 PDF는 이미 제출본이라 고치지 않았고 덱부터 반영한다.

**수치는 최신 검증치로 갱신했다.** 응급안전안심서비스 24만 → 약 30만 가구(2026.4, 라포르시안), 전국 고령
1인 가구 248만(pre.html) → 약 234만(2025 고령자 통계), 테스트 21개(사전과제) → 84개+E2E 12개(현재).
새로 확보한 근거 중 "2025년 응급안전안심 대응 38.5만 건의 91.4%가 사후 안전확인"과 "전북 접수 57.3%
오인·무응답 처리"가 '실시간 감지의 공백'을 공공 데이터로 보여주는 핵심 카드다.

**폐기한 경로를 두 번 세웠다.** 타겟 전환(독거노인→보호자, B2G가 원타겟을 회수)은 8번 슬라이드로,
음성 설계의 음량 기반 기각(신음을 '괜찮음'으로 오인)은 13번 슬라이드 각주로. 119가 실발신이 아니라
기록이라는 것도 덱에 그대로 명시했다 — 심사 질의에서 뒤집히는 과장을 만들지 않기 위해서다.

**차트는 dataviz 절차로 검증했다.** hipV 분리 차트에 쓰려던 중립 회색(#6b6a66)이 팔레트 검증에서 채도
부족 FAIL이라 일상=브랜드 블루/낙상=레드 조합으로 바꿔 전 항목 통과시켰다. 가격 비교 차트의 최장 막대
라벨(약 139만 원)이 뷰박스 밖으로 잘려 안쪽 배치로 수정했다. 슬라이드 스크린샷 검증에서 해시만 바꾸는
file:// 재방문은 리로드를 일으키지 않아(내비게이션에 hashchange 리스너 없음) show() 직접 호출로 찍었다.
