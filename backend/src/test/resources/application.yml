server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/fall_detection_test
    username: ${USER}
    password: ""
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

push:
  vapid:
    private-key: ""    # 셸에 VAPID 환경변수가 있어도 테스트에 새지 않게 고정
    subject: ""
