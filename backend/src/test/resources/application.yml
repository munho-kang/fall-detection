server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/fall_detection_test
    username: ${USER}
    password: ""
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
